# 不上傳為靜態資產的路徑(wrangler assets 用,語法同 gitignore)
.git
.gitignore
.claude
.assetsignore
.env
.env.example
.dev.vars*
# 本機 CLI／驗收輸出可能含部署回應、授權錯誤或暫存設定；不屬於網站資產。
# 2026-08-05 build 21 Preview 實際抓到這兩個未追蹤檔，故用磁碟現況做部署時必須明確排除。
firebase-debug.log
r.json
*.bak-*
.wrangler
.DS_Store
# TDX 歷史原始資料快取(建校正模型用,60 天約 50MB)。**實測從工作樹上傳時它會變成公開路徑**
# (2026-07-26 在預覽站抓到 .cache/tra_hist/2026-07-20.jsonl.gz 回 200/849KB)——原始資料不對外散布。
.cache
# 逐節點診斷檔(內部除錯用,前端不讀)
data/*_diag.json
# 站間距稽核報告(561KB、20311 行的內部分析,前端不讀;產物是 data/tra_seg_cumdist.json,
# 那份要留著公開——真接上位置推估時前端要讀得到)。2026-08-10 併入時補這條。
data/seg_distance_audit.json
# 驗收語料(保存的上游原始回應,如 2026-08-15 北捷斷線 40 輪共 11MB)。前端與 worker 都不讀,
# 只有 scripts/verify_*.mjs 在本機重放時讀。理由同上面的 .cache：原始上游資料不對外散布。
fixtures
wrangler.jsonc
worker.js
node_modules
package.json
package-lock.json
AGENTS.md
_config.yml
firebase.json
firestore.rules
scripts
app
docs
hand off
README.md
DESIGN_BRIEF.md
TODO.md
火車頭
design-mock.html
_專案資訊.html
授權查證*
安全審查*.md
# 對外洽談用的內部小抄／紀錄(含談判策略,禁上正式站)
*通話小抄*
國土測繪中心_*
ntmetro_fixtures
# 暫存驗證腳本(Playwright 等):慣例放 repo 根目錄好解析 node_modules,一律 ephemeral、禁上正式站。
# index.html 是純單檔 app、不載任何 .mjs 模組;正式 build 腳本都在已排除的 scripts/。
*.mjs
# 驗證腳本順手產的截圖(verify_*.mjs 每跑一次就重寫):同樣 ephemeral,不要變成公開資產
_shot_*.png
_baseline_*.html

# 內部設計／QA／文案文件(未追蹤,禁上正式站;2026-07-22 補齊)
QA測試報告*
商店文案*
Plus訂閱*
通知功能設計*
音樂功能設計*
*功能設計*
*系統設計*
研究_*
稽核_*
量測_*
使用說明*
交付_*
分析_*
參考_*
# App icon 概念稿(未追蹤,零引用,9.4MB)。2026-07-26 實測在預覽站全部回 200。
# 已追蹤的 icon 都不叫 app-icon-*,加這條不誤殺。
assets/app-icon-*
# 技術原型(3D 文湖線等):只在本機跑,不是產品的一部分,禁上正式站。
prototypes
# 各 session 的暫存工作區:中間產物、驗證輸出、交接草稿,內容不受控且多是未定案判斷。
# .gitignore 管不到 wrangler(它上傳的是磁碟檔案),這裡要另外列一次——2026-07-26 的 .cache
# 就是只進 .gitignore 沒進這裡,結果 TDX 原始快取在預覽站可下載。
scratchpad
# SDD(subagent-driven-development)的工作區:帳本、任務簡報、複審報告、以及每輪的完整 diff 副本。
# 與 scratchpad 同形——已進 .gitignore(.superpowers/sdd/.gitignore 一個 `*`),但同一課要記兩次:
# 從工作樹出貨時 .gitignore 完全不管用,這些會全部變成公開路徑(2026-08-02 稽核實測 81 檔)。
# 而且 review-*.diff 保存的是「改動前」的內容,就算敏感字串已在最終狀態被移除,diff 副本裡仍是原文。
.superpowers
# 小工具地點看板索引：只給 App bundle 用,網站不讀,9MB 不必當靜態資產外供
data/place_index.json
# 驗證腳本的工作目錄：binary 與中間 JSON，不是站台檔案（.gitignore 管不到 wrangler，見上）
tmp
# 外包施工單(內部作業文件,敘述了配額與帳號約束)。2026-08-03 差分實測:少了這條,
# 從工作樹部署的檔案數會多 1,它就變成公開路徑——同 .cache/scratchpad 的理由。
CODEX-*
