# 參與軌島

先說結論：**最有幫助的貢獻是回報問題與指正資料，不是寫程式。**

軌島把台鐵、高鐵與各捷運輕軌畫在同一張地圖上，位置多數是由時刻表與軌道幾何推演的。
這種東西最大的弱點不是程式寫得好不好，而是「哪裡跟現實對不上」——而那只有實際
在現場、在那班車上的人看得出來。目前 repo 上品質最高的貢獻，全部來自 Issue。

參與前請先看 [LICENSE](LICENSE)：軌島是**原始碼公開（source-available）**，不是開源軟體。

---

## 一、回報問題（最歡迎）

開 [Issue](https://github.com/siriushsu/taiwan-rail-live/issues) 即可，不必客氣。
附上這幾項會讓問題快很多：

- **BUILD 版本**——畫面上時鐘徽章旁的版本戳記（或 console 第一行）。
  很多「還沒修好」其實是分頁開太久跑到舊版，先對版本能省掉一整輪。
- **哪一班**——系統／路線／車次號，或捷運的哪一條線、哪個方向。
- **什麼時間發生**——台北時間，精確到分鐘。位置是依時刻表推演的，
  沒有時間就重現不了。
- **截圖**——尤其是位置、標籤、版面類的問題。
- **裝置與瀏覽器**——特別是 iPhone／Safari。軌島踩過好幾個只在 WebKit 出現、
  在 Chrome 完全重現不了的 bug，這一欄常常是關鍵。

### 資料錯誤特別歡迎

軌道畫錯、站點位置偏了、平交道不在那裡、班次時刻對不上、車種標錯——這些請直接開 Issue。
如果知道正確資訊的來源（官方時刻表、現場照片、OSM 節點編號），一併附上。

平交道位置依台鐵官方資料標示，已知可能與現場有落差，這類回報一律歡迎。

---

## 二、Pull Request

小修（錯字、明顯 bug、資料修正）可以直接開 PR。
**動到功能、版面或資料管線之前，請先開 Issue 談**——避免你做完了才發現方向不合。

實務上要知道的幾件事：

- **前端是單一檔案**：`index.html` 含全部 HTML／CSS／JS，**沒有 build step**。
  起 server 就能開：`python3 -m http.server 5178`。
- **資料在 `data/`，抓取與建置腳本在 `scripts/`**。資料檔請不要手改——
  它們是腳本產生的，手改會在下次重抓時被蓋掉。要改請改腳本。
- **有驗收腳本**：`verify_*.mjs` 是 Playwright 實跑的行為驗證。動到相關區域請跑過。
- **手機版一定要驗**：軌島的使用者以手機為主。改任何 UI 請至少在 360／375／414／768
  四個寬度看過，並實際點擊測試——不要只看截圖有沒有重疊，要確認「點下去發生什麼」。
  曾經有兩顆並排的按鈕在幾何上完全不重疊，觸控熱區卻整條蓋掉彼此。
- **繁體中文**：介面文案、commit message、註解都用繁體中文。

送出 PR 即表示你同意 [LICENSE](LICENSE) 第五節第 2、3 款的貢獻條款（重點：你授權作者商業化
你的貢獻，包含未來的付費版本）。這一條寫得直白，是希望你在動手前就能決定要不要。

---

## 三、想做 Android 版、移植或獨立發布？請先開 Issue

**先講：這個想法本身沒有錯，而且技術判斷是對的。**
`app/` 是 Capacitor 專案，`android/` 只是被 gitignore 的生成目錄，`npx cap add android`
就能跑起來。從程式碼看，Android 確實不難。

難的是三件從 repo 看不出來的事：

1. **完整版做不出來。** 線上底圖（Stadia Maps、Esri）的金鑰不在 repo 裡，由伺服器端下發；
   發行流程還有一道授權閘門（`release-policy.json`，同樣不在 repo）。少了這些，
   build 出來會是缺底圖的殘版。

2. **`suno musics/` 那 29 首不能跟著走。** 它們的商用權綁定「由作者本人於 Suno 訂閱
   有效期間生成」，**不授權給任何第三方**。打包進另一個 app 並發布，是發布者自己承擔
   侵權責任——這一點很容易在不知情的狀況下踩到，所以特別寫出來。

3. **資料需要持續維護，不是一次性的。** 台鐵班表是 14 天滾動窗口，每週要重抓；
   每次改點還要重新驗證校正模型。一份 fork 撐不過兩次改點就會開始給出錯的時刻，
   而使用者分不出那不是官方版本。

再加上一件現實問題：如果衍生版本先上架，之後正式版上架時會撞商店身分與品牌，
使用者也會混淆。**這不是不信任你，是這幾件事一旦發生就不好收拾。**

### 但你的熱情有更好的去處

如果你想幫 Android，這兩種形式現在就能開始，而且是真的缺：

- **真機測試。** 由作者出 build、你在自己的裝置上實測回報。
  真機涵蓋面是目前最缺的一塊——iOS 那邊已經吃過好幾次「模擬器全過、真機才爆」的虧，
  Android 的廠牌差異只會更大。
- **Android 特有的差異。** 返回鍵行為、通知與前景服務、Live Activity 的 Android 對應、
  各廠牌省電策略對背景行為的影響。這些是 iOS 經驗完全遷移不過去的部分。

開一個 Issue 說你想做哪一塊，我們把範圍講清楚再開始。
**已經動手做了一部分？也請開 Issue 說一聲**，沒關係——沒有 LICENSE 檔的期間，
會以為可以自由 fork 是完全合理的。我們一起看怎麼讓那些工作不白費。

---

## 四、其他

- **商標**：「軌島」「Rail Island」名稱、App icon 與品牌識別不在授權範圍內，
  請勿用於自己發布的產品，或以任何方式使人誤認有關聯。
- **安全性問題**：請**不要**開公開 Issue。寄到 support@railisland.tw，
  給一點時間修好再公開。
- **商業合作、資料授權、例外授權**：support@railisland.tw。

謝謝你願意花時間看到這裡。軌島是個人專案，每一則認真的回報都會被讀。
