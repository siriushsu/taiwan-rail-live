# 視角轉場與地點導覽 — 2026-09-14

從本機整合 v26 移植到 `origin/main` aba8f003 的原生 MapLibre 版本，BUILD v0914b。保留正式版實體股道、夜景、地表／軌面高度、載入世代取消、按需重繪與手機嚴格車頭鎖定；不部署原型伺服器或未追蹤資料。

新增更多面板的俯視／傾斜連續過場、12 個站區與 5 個地標導覽，以及一般跟車各角度的近景構圖。UI、導覽地名及更新紀錄補齊英文與日文，i18n 稽核也新增導覽模組與目錄覆蓋。

本機 Chromium／WebKit 新功能驗證各 26/26，既有車頭鎖定真實捏合／旋轉／拖曳與放空回歸 32/32。手機寬度 360/375/414/520/768，另含桌面 1280；新控件以 elementFromPoint 查可及性，掃同一模態層及仍可互動的外部控件，排除被抽屜遮罩覆蓋的背景操作。台北 101 手機實際模型框 x=125.6–247.1、y=255.4–609.0，完整在 375×900 畫面內。啟動曾有兩次逾時，重新獨立啟動及完整雙引擎重跑皆正常、無前端例外；不宣稱已定位該暫態原因。

出貨前正式版：BUILD v0914a，Cloudflare version a4d40432-8ac7-4b5a-a472-7301db215733（回復目標）。出貨仍走 `scripts/ship_web.mjs` 的全部 gate 與等價去註解程序。新功能驗收可用 `BASE_URL` 指向出貨檔或正式站。
