# 交接：北捷車廂位置「跳動／重新判斷／消失」

> 建立 2026-08-14。工作樹 `.claude/worktrees/trtc-binder-fix`，分支 `feat/trtc-join-parity`。
> 基底 `dab284a`（＝當時的 `origin/main`）。已完成的一顆＝`14f3724`。

## 0. 使用者裁示（逐字，不准重新討論、不准設 gate 要求先證明才准做）

> 「我們重點是要依照官方即時資料，即使班表沒有對上也沒有關係，我們的地圖顯示就是依照
> 即時資訊，班表只是沒有資訊的備案。所以我才一直說車子怎麼可能會不見，我們不做判定。」

> 「現在台北捷運的車廂位置有很大問題，不是對不對，而是不斷跳動、重新判斷、消失。
> 其實有車號的時候位置是對的，但是資料讀取的時候車子會一直被判斷不在那邊。
> 請維持有訊號時的推算久一點，車子不要一直在中間消失，只要到站、出發後，就一直維持速度，
> 準時到下一站，這樣就好。」

相關長期裁示：站牌時間完全照官方；位置不用準、時間一定要準；不要每次打開都重算一次；
官方給的值一律照抄字面。

**這幾條是裁示不是假設。** 不要設計「先證明比較準才准上」的閘門，那是上一輪已經犯過的錯。

---

## 1. 現況量測（正式站，2026-08-14 早尖峰，30 輪 × 5 秒）

探針在 `scripts/probes/`，三支都是唯讀、直接對 `https://railisland.tw/api/trtc-live` 拉，
不需要金鑰也不寫任何東西回去（產物落在 `./tmp/`）：

| 探針 | 量什麼 |
|---|---|
| `node scripts/probes/probe_roster.mjs <輪數> <間隔ms>` | 每輪車數、資料齡、gone／born／moved／reanchored／past |
| `node scripts/probes/probe_jump.mjs <輪數> <間隔ms>` | 同一台車同一區間，位置在同一瞬間位移了多少 |
| `node scripts/probes/probe_churn.mjs <輪數> <間隔ms>` | 消失的車是「真的不見」還是「換號」（見第 3 節） |

**要在營運尖峰跑才有訊號**（離峰車少、feed 也穩，量不到問題）。

量到的四個症狀與機制：

| 症狀 | 機制 | 數字 |
|---|---|---|
| 到站後不動 | `now >= arrEpoch` 直接凍在 `to` 站 | 每輪平均 **70.9／186 台（37%）** 已越過自己的 arrEpoch |
| 整批消失 | `now - sourceRevision > 30` 讓整份名冊失效 | 資料齡 max **32.9 秒**，30 輪中 **2 輪**超標 ⇒ 那兩輪全線的車同時不見 |
| 不斷跳動 | 官方每輪重給 ETA，同區間位置瞬間位移 | **494 次**；中位 8%、p90 **34.1%**、p99 87.7%、max **100%** 的站間距；其中 **212 次往後退**；3 次非相鄰跳站 |
| 中間消失 | **前端讀不到就整包清空**（見下）＋後端名冊配不到就刪車 | 30 輪內 73 台消失／80 台新生；40 輪版：248 台消失＝到終點站 11 ＋ 換號 18 ＋ **「真的不見」219** |

**「真的不見」那 219 台的真相**（`probe_group.mjs 14 8000 BL 1` 拍到的）：
BL 往西 15 台車，第 9 輪與第 12 輪 **`rev: null`、`n: 0`、整包讀不到**，下一輪 15 台原封不動
全部回來。也就是 **14 次輪詢有 2 次讀不到（約 14%）**，而舊碼一遇到讀不到就
`trtcOfficialRosterOutage('fetch-error')` ⇒ 整條線的車同時消失、連跟車都被
`clearFreqFollow()` 清掉。使用者說的「資料讀取的時候車子會一直被判斷不在那邊」講的就是這件事。

⚠️ **量到的關鍵否決證據**：若照原本的直覺「配不到就一律續推 600 秒」，
以實測的消失率外推，同時掛著的幽靈上限約 **411 台，而全隊只有 170 台**。
**不准做無條件的後端續推。**

payload 實況（做設計前先確認過的）：`boardPos.vehicles[]` 帶 `history`（186 台中 138 台
有 ≥2 筆）、`dest`、`dir`、`run`（中位 95 秒、p90 172 秒）、`tripKey`。
**注意 `run` 可能比 history 相鄰兩筆的間隔還大**（實測有 `run=109` vs `span=90`）。

---

## 2. 已完成並驗過的（前端，commit `14f3724` ＋ `2` 顆）

`index.html`：

- `TRTC_OFFICIAL_COAST_MAX_SEC = 600`（index.html:4028），
  `TRTC_OFFICIAL_ROSTER_MAX_AGE_SEC` 改成等於它。
  **語意換了**：資料齡不再是「整批停用」的開關，是「最多再續推多久」。
- `trtcOfficialCoastCycle(vehicle, run)`（index.html:4508）：週期優先取這台車自己相鄰兩筆
  官方到站的間隔，沒有 history 才退回 `run + 25`；停站夾在
  `[TRTC_OFFICIAL_COAST_DWELL_MIN_SEC=15, 週期/2]`，避免 run > 週期時算出負的行駛時間。
- `trtcOfficialCoastPosition(...)`（index.html:4520）：越過 `arrEpoch` 之後照上面的週期
  一站一站推下去（停站→行駛→準時到下一站），推到 `dest` 就停在終點站，
  `elapsed > 600` 才回 `null` 交還班表。回傳多帶 `coastTo` / `coastArrEpoch`。
- `trtcOfficialVehiclePosition` 的到站分支改成呼叫它（index.html:4563，
  錨點註解 `ROSTER_FRONTEND_POSITION` 與 M-B 的突變字串都刻意原樣保留）。
- `trtcOfficialVehicleInfo`（index.html:4571）改用 `coastTo`/`coastArrEpoch` 報下一站與倒數。

### 2-2 讀取失敗不得清空名冊（H 閘門）

- 新增 `trtcOfficialRosterHold(reason)`（index.html:16203，錨點 `ROSTER_FRONTEND_READ_HOLD`）：
  名冊還在且是 `official` 時，**名冊物件一個位元都不動**，只在
  `state.trtcOfficialRosterHold` 留診斷；名冊本來就沒有才退回 outage。
- `pollTrtcLive` 的三個讀取失敗理由改走 hold：`fetch-error`（index.html:16687）、
  `missing-boardPos`（16670）、`frontend-apply-error`（16676）。
  **`time-travel` 仍是 outage**（那是刻意交還班表，不是讀取失敗）；
  `feed-outage`／`malformed`／`source-revision-regressed` 也維持 outage，本輪沒動。
- 到期只剩一個權威：`trtcOfficialRosterActive` 的 `TRTC_OFFICIAL_COAST_MAX_SEC`。
- ⚠️ 判準更新：`integrationSourceAudit` 原本硬性要求 `pollTrtcLive` 內出現
  `trtcOfficialRosterOutage('fetch-error')`——那正是要廢掉的行為，已改成要求 hold；
  完整契約由 H 閘門承接（三個理由都走 hold／都沒漏回 outage／時光機仍是 outage／
  runtime 實測名冊參照與 vehicles 陣列都沒被換掉、跟車沒被清）。

`scripts/verify_official_roster_frontend.mjs`：新增 **G／H 閘門**與
**M-G1~M-G4、M-H1、M-H2** 六發突變。

```bash
cd /Users/xuxiang/Code/捷運小動畫/.claude/worktrees/trtc-binder-fix && node scripts/verify_official_roster_frontend.mjs
```

現況：**斷言 44/44、mutation 28/28、browser 8/8**（chromium＋webkit × 360/375/414/768）。

（`pollTrtcLive` 是 async，`extractFunction` 從 `function` 起算，
所以 M-H1 不能走 `mutationIndexFunction` 的 `(${mutant})` 包法編譯，要自己補回 `async`。）

G 閘門驗的十三件（判準只用站點座標與手算的時刻常數，**不重跑實作公式**）：
到站當格 bit-exact／停站中留在原站／停滿就開走／行駛中點在正中央／準時到下一站與再下一站／
終點站停住不衝過頭／續推上限 600 秒當格仍在而 601 秒交還班表／逐秒單調不倒退／
反方向同樣成立／沒有 history 的退路／`run` 大於週期不算出負停站／已在終點站的車不動／
面板的下一站跟著前進。

---

## 3. 還沒做的兩件（照這個順序）

### 3-1 後端：配不到的車不要直接刪掉（`scripts/trtc_official_roster.mjs`）

> **先重量再決定要不要做。** 第 2 節的 hold 修好之後，使用者看到的「消失」有多少還剩下
> 是未知數——40 輪裡「真的不見」的 219 台，主因已經證實是前端讀取失敗而不是後端刪車。
> 重跑 `probe_churn.mjs 40 8000` 前不要動這一節。

`reduceOfficialRoster()` 尾段（trtc_official_roster.mjs:296-307）對「上一輪有、這一輪配不到」
的車只留兩條活路：已經是 extension 且還沒到 `arrEpoch`，或 `finalExtension()` 成立
（**只救倒數第二站→終點站那一段**，且要 `history.length >= 2`、
`0 < ownLastSpan <= OFFICIAL_OWN_SPAN_MAX_SEC(600)`）。其餘一律 `exits++` 刪掉。
這就是「中間消失」的來源。

**做之前一定要先跑 `probe_churn.mjs` 判斷這 73 台裡有多少是「換號」不是「真的不見」。**
理由：`born` 有 80 台，同一輪 gone 73／born 80 的形狀高度懷疑是 `alignOrdered()` 的身分
churn——若把換號的那些也一起續推，等於**每次換號都留一隻幽靈，車數會膨脹**（全隊才 186 台）。
判準寫在探針裡：同一 `line|dir|dest` 群組中，有 born 的車 `routePosition` 落在
`[gone.rp, gone.rp+2]` ⇒ 算換號。

建議的續推條件（三個都成立才留）：
1. `to !== dest`（到終點站本來就該退場，那不是「中間消失」）；
2. 這一輪同群組沒有新車頂替它的位置（否則是換號，留著會變兩台）；
3. `nowEpoch - arrEpoch <= TRTC_OFFICIAL_COAST_MAX_SEC(600)`（與前端續推上限同一個數，
   超過前端本來就畫不出來）。

留下來的車**內容原樣不動**（`from/to/run/arrEpoch/history` 全保留，只更新 `sourceRevision`，
另加 `carried: true`），位置交給前端第 2 節的續推算——**不要在後端再算一次位置，
兩邊各算一次一定會漂**。

守恆式 `vehicles.length !== current.length + extensions`（trtc_official_roster.mjs:310）
要一起改成含 carried 的數，不要繞過它。

驗收：`scripts/verify_official_roster.mjs`（後端那支）要加
「配不到但還沒到終點的車仍在名冊」「換號的車不會變成兩台」「超過上限就退場」三條具名斷言，
各配一發突變；**突變要先宣告預期轉紅哪幾條再跑**（這支腳本已有 MUTATION_PLAN 的慣例）。

### 3-2 前端：位置平滑層（「不斷跳動」）

現在 `trtcOfficialVehiclePosition` 是純函式，官方一改 ETA 位置就瞬移（494 次、212 次往後退）。
使用者要的是「不要一直跳」，不是「更準」。

設計（已想過但**還沒寫任何一行**）：

- 讓位置函式每個分支都回一個**沿路線單調遞增的純量** `prog`：
  `step = dir===2 ? 1 : -1`；站上＝`step*站序`，區間內＝`step*from + fraction`。
  已驗證這個量對固定的一筆 vehicle 記錄在 `now` 上是連續且單調遞增的（續推段也是）。
- 另開 `trtcOfficialDisplayPosition(ln, vehicle, now)` 當**唯一的顯示入口**，
  內含一個 `Map` 記 `${lineId}|${vehicleId} -> { prog, now }`：
  - 往後退 ⇒ **凍住不倒退**（＝台鐵 easedShift 那層的既有做法：誤點上升就凍結）；
  - 往前跳 ⇒ 限速追上，最多 `(1+3)/run` 段/秒（約 4 倍自然速度，34% 的落差約 8 秒補完）；
  - `now` 與上次差太多（切分頁、休眠）就直接重新對齊，不要慢慢爬。
  - 時鐘不在「現在」（時光機）時**不套平滑**，直接回純值。
- **`trtcOfficialRenderItems`（index.html:4578）與 `trtcOfficialVehicleInfo`（index.html:4568）
  必須走同一個入口**，否則畫面用平滑值、跟隨相機用原值，跟車會偏心。
  命中測試走 render items，自動一致。
- 純函式 `trtcOfficialVehiclePosition` 保持不變 ⇒ 現有 B／G 閘門與「arrEpoch bit-exact 到站」
  全部不受影響。

驗收要加的斷言：連續 `now` 序列中 `prog` 單調不遞減；注入一筆「往後退 34% 的 ETA 修訂」後
畫面位置不倒退且在 N 秒內收斂到新值；往前跳 100% 時每秒位移有上界。
突變：把凍結拿掉、把限速拿掉、把時光機豁免拿掉，各要轉紅。

---

## 4. 規矩（別踩）

- **驗收器的 VM bundle 是白名單**：`buildProductApi()`（verify_official_roster_frontend.mjs:125）
  與 `followRuntime()`（同檔 ~341）用 `extractConst`／`extractFunction` 把具名符號抽進 `vm`。
  新增任何 module-level const 或 helper，只要被抽進去的函式引用到，**就要同步加進 bundle**，
  否則整支腳本 ReferenceError 中止——而那個中止**不算轉紅**，會讓人以為驗過了。
- 錨點註解每個只准出現一次（腳本開頭有 gate）：`ROSTER_FRONTEND_ACTIVE`／`_POSITION`／
  `_RENDER`／`_GLYPH`／`_TARGET`／`_GEOMETRY`／`OFFICIAL_PAYLOAD_SCALAR_GUARD`／
  `OFFICIAL_PAYLOAD_TIMING_GUARD`／`OFFICIAL_POSITION_AXIS_GUARD`／
  `ROSTER_FRONTEND_REVISION_HIGH_WATER`。
- M-B 的突變字串是 `'if (now >= arrEpoch) return'`，M-A2 是
  `'now - revision <= TRTC_OFFICIAL_ROSTER_MAX_AGE_SEC'`——改到那兩行時要保留字面。
- 判準不要綁在會漂的量上，也不要與實作同源（本輪 G 閘門就是照這個寫的）。
- **更新紀錄**：首層 `data-cl-of` 上限 8 條、正本 `data-cl`，由
  `verify_plus_subscription.mjs` 的 CL0/CL1/CL1b/CL2 把關；只寫「真的到過使用者手上」的缺陷。
- **部署**：這是功能批次，**必須先上預覽站給使用者親試、拿到明確 go 才升正式站**。
  wrangler 一律 `arch -arm64 node ./node_modules/wrangler/bin/wrangler.js`，
  從乾淨 detached worktree 出貨（`.gitignore` 對 wrangler 無效，只有 `.assetsignore` 有效）。
  驗線上內容要帶 `-H 'Cache-Control: no-cache'`（`?cb=` 對 static assets 無效）。
- App 端：`~/Desktop/軌島_1.4.3_build49.ipa` 已建置驗畢待使用者上傳，**48 作廢**。
  這批修好之後要重出 build 才會進 App。
