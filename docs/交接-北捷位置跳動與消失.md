# 北捷官方列車生命週期（唯一有效契約）

> 2026-08-14 定案。這份文件取代 2026-08-13 的官方名冊／到站稽核與未驗證續推草稿；
> 被取代文件已移除，不得從 git 歷史把其中的時間刪車、班表主體或重新配對規則搬回來。

## 產品規則

1. 起點倒數第一次出現時建立一台車並分配 `vehicleId`。冷啟動若剛好在營運途中，允許用
   當下官方列建立一次初始名冊；每台車都必須保存可稽核的官方站牌出生證據。初始名冊建立後，
   站間未配列不得另生一台車，班表與內部推算都沒有生車權。
2. 同一台車會同時出現在多個車站的倒數裡。相同路線、方向與官方車號的列要合成同一台車的
   逐站 `timeline`，不是各畫一台。沒有官方車號的文湖線／環狀線，沿行進方向的同車到站
   epoch 必然逐站遞增；epoch 回落才是下一台車的邊界。不得再用每一站各自的單段秒門檻生車。
3. 官方車號只是 `vehicleId` 上的顯示標籤，不能決定位置。已認到的號碼若後續變動、消失，或
   同號列出現在物理上不可能到達的遠處，保留原 `vehicleId` 與位置並永久拿掉該趟號碼，畫面
   退回路線縮寫橢圓牌；不得換成新號，也不得為保住號碼把車拖去遠處。抵達終點後，即使原車
   折返且沿用相同車號，也必須等反方向起點倒數出現後建立新的 `vehicleId`。
4. 車一旦建立，後續資料只能更新它。整包讀取失敗、空列、畸形資料、來源版本倒退、單列配對
   失敗、目的地改標及 ETA 前後修訂，都不得刪車、換 ID、複製車或清除跟隨。
5. 每段位置只依該車官方 `timeline` 的出發／到站秒推算。ETA 往回修時，畫面不得倒退；往前修
   只能把新位置當成目標，任何一畫格、15 秒輪詢、頁面停頓或到站 deadline 都不得讓顯示進度
   快過該站間的實際行車秒數。舊的「每 10 秒追一站」與 deadline 強制吸到站規則已完全移除。

   **唯一例外（2026-08-15 使用者裁示）：長時間斷訊回歸。** 續推持續滿
   `TRTC_OFFICIAL_RESYNC_MIN_COAST_SEC`（5 分鐘）以上的車，一旦官方時間軸回來，必須立刻改採
   官方位置，允許往前跳、也允許往後退，並重設單調基準。原文：「不倒退應該是之前我們定下的，
   但是在這個斷訊回歸的狀況下，我可以允許車輛倒退甚至跳動。這只有在長時間斷訊回歸的時候
   可以發生，而且需要跳出訊息告知使用者為何有這個狀況。」不到 5 分鐘的短暫缺訊不適用——
   偏差本來就小，不值得讓畫面跳。
6. 一般路線只在車抵達倒數第二站、官方資料確定缺少最後一段時續推。**續推節奏一律用該路線
   `ln.segs[].run` 的固定行車秒＋固定停站秒（`TRTC_OFFICIAL_COAST_DWELL_SEC`）逐段走，不得用
   單一車自己量到的站間週期外推整段剩餘旅程。** 2026-08-15 斷線 33 分鐘實測：per-vehicle
   `coastCycle` 在同一條線上分布 1～1681 秒（18 倍以上），板南線 214 以 192 秒/站、217 以
   100 秒/站各跑各的，晚 7.5 分鐘發車的 217 反而超到 214 前面一站。固定段秒讓全線同一個節奏，
   結構上不可能互相超車。沒有 `segs` 資料的線（環狀線 Y）才退回 per-vehicle 週期，這是已知
   缺口、不是預設路徑。不得預先用整份靜態班表憑空推完整趟存在性。
7. 小碧潭與新北投支線是兩站單段例外：實測官方資料只有起點倒數，沒有對端到站列，所以使用
   靜態單段行車秒數跑到另一端，抵達即收車。
8. 唯一退場條件是抵達這一趟已知終點。沒有資料齡、缺訊秒數或「太久沒看到」的退場判斷。

   **2026-08-15 使用者裁示補充（逐字）**：「太久沒看到就刪車 還有之前不要認不到就消失的規則
   那是平常判斷時不能用 但是在這種斷訊半小時的狀況下，首先我們要避免位置推算偏離那麼多
   再來再訊號恢復時，我們要能夠抓到實際上的列車數量與位置才行。」
   ⇒ 界線：**平常營運**維持本條與十一條廢棄規則原樣；**長時間斷訊**是明確的例外區——
   (a) 斷訊期間位置推算不得偏離那麼多（規則 6 的固定段秒續推）；
   (b) 訊號恢復時必須收斂回官方的**實際列車數量與位置**：恢復後的名冊對帳被明確授權，
   包括收掉官方資料不再支持的斷訊殘車與重複出生車。對帳的觸發條件是「剛從長時間斷訊
   恢復」這個事件，不是平常的資料齡門檻——不得把這條反過來當成日常的逾時刪車。
9. 班表只在從未取得官方車輛資料，或使用者把模擬時鐘撥離現在時，作為整體備案；班表不得
   決定官方車的存在、身分或退場。
10. 車牌有仍可信且前後一致的 `TrackInfo.TrainNumber` 時顯示該官方欄位；沒有或已發生衝突時
    沿用路線縮寫橢圓牌（文湖線為 `BR`、環狀線為 `Y`）。不得顯示內部 `vehicleId`，也不得把
    無法可靠對回站牌的車廂資料編號冒充車次。
11. 每個顯示位置都要攜帶當下實際行駛的 `motionFrom→motionTo`。位置進度與方向箭頭以這一段為
    唯一方向來源，不得另信可能晚一輪的名冊 `dir`，也不得用跨畫格角度平滑把箭頭留在車尾。
    其他系統共用的箭頭若舊角度與新移動向量夾角已達 90°，必須立即採新方向。
12. **官方訊號中斷必須讓使用者看得到（2026-08-15 使用者要求）。** 原文：「在北捷斷線的時候
    應該要有更明顯的提示 告訴使用者現在位置跟倒數可能不準。原因是因為北捷訊號中斷。」
    資料齡一律由前端從名冊 `observedEpoch` 自己算，**不得讀 `boardPos.held`**——後端從來沒送過
    那個欄位，2026-08-15 斷線 33 分鐘期間徽章因此全程亮著「官方即時」。中斷滿
    `TRTC_FEED_STALE_SEC`（3 分鐘）時，⚠ 公告（手機鈕／桌面橫幅，排在清單第一則）、捷運徽章、
    車站看板警語、跟隨小卡狀態四處都要說明「北捷訊號中斷，列車位置與車站倒數都可能不準」。
    新鮮度**逐上游分開算**：環狀線走新北捷另一支上游，一邊斷不得誤報另一邊；分組單位是上游
    不是線，否則某條線的車全部續推過終點被收掉時，那條線反而不算中斷。
13. **斷訊回歸時必須跳訊息說明畫面為什麼會跳（規則 5 例外的配套）。** 回歸事件由位置函式記錄、
    每秒 tick 消費後 `showToast`；同一波回歸只發一次，內容要講清楚中斷多久、多少台車被對回
    官方位置、以及「所以這些車會往前跳或往後退」。位置函式本身不得直接呼叫 UI。

## 實作邊界

- `scripts/trtc_board_ledger.mjs` 先把無車號列依逐站 epoch 的單調區段分群，再由
  `attachOfficialTimelines()` 把同群倒數附加到同一台車；倒數列數不能改變車輛基數。
- `scripts/trtc_official_roster.mjs` 是唯一生命週期 reducer。正常營運只有起點事件可出生，
  既有車沿自己的 `timeline` 保留到 `retireEpoch`；兩輪配對另以實際經過秒數與段秒驗證可達
  距離，官方車號不得略過這道閘門。無車號車先以兩輪重疊的逐站 ETA 辨認身分，再比較沿線
  順序；相鄰站 ETA 差與真正經過的觀測秒相加仍跑不完該段時，必須拒絕配對。
- `worker.js` 把同一份 canonical timeline 寫入持久名冊與回傳 payload；frame key 也包含 timeline，
  避免同 revision 的逐站時間修訂被吃掉。TrackInfo 讀取失敗或本輪組裝失敗時，必須回傳
  D1 最後成功官方名冊作為 `official` hold，不得回空 `outage`；這條規則也適用於剛開頁面、
  前端尚無上一幀的情形。只有 D1 從未建立官方名冊時才能回班表備案。
- `index.html` 的純位置函式負責官方時間軸，顯示層另以 per-vehicle 單調進度消除 ETA 回修造成
  的倒退，並逐段累計 `runBetween()` 限制最大前進距離。位置物件的 `motionFrom→motionTo` 同時
  驅動顯示進度與箭頭切線；官方箭頭直接採該切線，不進跨畫格 EMA。資料失敗只標記 hold，
  不清空最後成功名冊。
- 舊班表校正路徑仍可替班表動畫算 shift，但它的「配不到」只代表那筆 shift 不採用，絕不可
  影響官方名冊。

## 必跑驗收

```sh
node scripts/verify_trtc_official_roster.mjs
node scripts/verify_trtc_official_worker.mjs
node scripts/verify_official_roster_frontend.mjs
node scripts/verify_trtc_outage_coast.mjs   # 2026-08-15 斷線語料重放：固定段秒零超車＋回歸重對齊
node scripts/verify_trtc_outage_ui.mjs      # 斷線提示四處可見＋回歸 toast＋按掉不再彈（真瀏覽器）
node scripts/verify_trtc_ghost_fix.mjs      # 2026-08-15 真語料 40 輪：零組裝例外＋名冊健康＋同 frame 冪等
```

### 🔴 2026-08-15 幽靈車事故：根因與修法（已定案）

**根因不在生命週期契約，在管線更上游的一個組裝缺陷。** `attachOfficialTimelines()` 的接鏈
迴圈會把**終點列的 `depEpoch` 改寫成前一筆的到站時刻**；終點列依定義 `dep === arr`（車還停
在起點），於是做出 `dep < arr` 的非法列 ⇒ `reduceOfficialRoster()` 當場擲
「official roster timeline 形狀不合法」⇒ worker catch 住整輪丟掉、退回
`trtcOfficialHeldPayload(env, 'assembly-error')`。觸發條件是**同一起點同時排兩班發車**
（官方站牌本來就會這樣列），所以它天天都在發生，不是斷訊才有。

實測證據（`fixtures/trtc-outage-20260815/rounds/` 40 輪）：

- 語料 40 輪＝**assembly-error 22 輪**＋上游斷訊 2 輪＋正常 16 輪；亦即正式站當天
  **38 個有效輪次丟了 22 輪**。離線重放**逐輪對照 40/40 一致**（唯二差異是上游整包失敗、
  production 根本沒跑管線的那兩輪）。
- 補上 `if (current.terminal) continue;` 之後 40 輪**零例外**；冷啟動重放整段幽靈車隊時間窗，
  末輪 106 台、逐輪 99–109、累計 births 138、**重複出生證據 0 組**。

丟掉超過半數輪次同時解釋了兩個症狀：名冊停在舊 revision ⇒ **位置持續偏離**；恢復期每次成功
輪面對的都是老舊 prior ⇒ 端點配對失穩 ⇒ **重複出生**。

同批第二根因（Codex 複審指出、對照組確認**修 assembly 之前就存在**，不是這次改出來的）：

- **同一份官方出生證據被重放時會第二次生車**。worker 的 CAS 重試會把已套用過的 frame 疊回
  已經前進的名冊（`trtcOfficialNextState()` 的短路只比對 prior 的 frameKey，重試時 prior 已換人），
  原車離開起點後配對判為不可行 ⇒ 同一列再生一台。修法在 reducer：
  `priorBirthSignatures` 擋掉「出生證據與既有車完全相同」的出生，`occurrence` 當鑑別子。
  **真語料上是可證明的 no-op**（健康 40 輪逐輪車數與關掉閘門逐一相同、blocked=0），
  只在污染狀態咬人。

**❌ 兩項試過但退回、不要再加回來**：

1. **「完全相同的 reducer 入口列合一」**——這違反使用者裁示「車子有官方數據就是在」。無車號
   路線（BR／Y）的起點列會出現逐 byte 相同的兩筆，上游 `collapseClaims()` 原樣送過來
   （實測 raw 2 → resolved 2 → claims 2 → collapsed 2），合一就是**少畫一台真車**。
   我曾以「真語料上一列都沒合到」推論它安全——那是事實但推不出結論，該情境當天沒發生而已。
2. **「端點 occurrence 專用配對」**（起點 pending ID 先於全線 DP 分配）——貪婪搶 ID 會讓
   `alignOrdered()` 的最優配對失效，起點身分的延續本來就由 DP 負責。註：先前此處寫的
   「多出 6 台」複審時重量不出來，**該數字不可引用**；移除的理由是機制本身，不是那個數字。

### 部署順序鐵則

前端固定段秒續推**不得先於上述後端修正單獨部署**：正式站 v0814j 對端點疊車推不動（`run=0`
使舊續推失效 ⇒ 堆在端點），本分支的續推**會把它們一台台推上線**（07:44 名冊實測 +15 分鐘
34 台無號幽靈在路上跑）。**worker 與前端必須同輪上線**。若上線時 D1 仍帶污染狀態，比照
v3→v4 前例升 roster schema／D1 key 隔離（本次事故當下已直接刪 `official_roster_v4` 冷啟動
重建，正式站已乾淨）。

驗收必須同時包含：

- 同車多站倒數只產生一個 ID，兩個方向各有案例。
- 無車號逐站倒數的 epoch 單調區段只產生一台；下一班起點倒數不得被吞進前車。
- 同號列若在 15 秒內從永安市場跳到丹鳳不得接回；號碼衝突後同一 ID 永久退回路線代號。
- 已認到的車次號遇到後續站牌暫時空白必須沿用；空白不是衝突，只有不同的非空號碼才永久退牌。
- 新開頁面的第一輪 TrackInfo 就失敗時，必須從 D1 讀回已知車、原 `vehicleId` 與已認到的車次；
  只讀不寫、不重發 ID，也不得整批退回班表。
- 當輪只有遠方同號列時，遠方列不得出生；舊車沿既有官方時間線保留，但號碼立即退回路線牌。
- 每台現存車都要帶 `official-board` 出生證據；任何缺少血緣的車讓驗收直接失敗。
- 空輪與長時間缺訊不刪車；目的地與 ETA 修訂不換 ID。
- 站間未配列不出生；反方向即使同車號也用新 ID。
- 一般線補唯一末段；G_XBT／R_XBT 四個方向走單段例外並在對端退場。
- ETA 回修位置不倒退；前修只能在實際段秒上限內前進，互相矛盾的到站秒不得強制吸到站。
- 方向箭頭必須取目前顯示位置的 `motionFrom→motionTo` 路線切線，不得另讀名冊 `dir` 或修訂前
  的 ETA 時間線；驗收要刻意讓 `dir` 與兩個方向的移動段互相矛盾，箭頭仍須跟著實際路段。
  投影向量不得依賴目前倍率下的像素距離，zoom 6／7 也要立即指對，不可等放大才自癒；共用
  箭頭也要證明舊角度一旦落在新向量反面會立即轉正。
- 畫面停頓超過 5 秒後，只能依真正經過秒數往前；不得直接跳到新原始位置。新 ETA 若落在
  已顯示位置後方仍須守住，禁止倒退重設。
- BR 無號多車跨輪必須各自接回原 ID、維持順序，不得交叉或飛奔；兩個方向都要驗。
- BR 兩個方向都必須證明 15 秒內不能把同一 ID 推進完整一站；保存語料中任何跨輪前進都要通過
  「相鄰到站 epoch 差＋真正經過秒 ≥ 該段行車秒」，任何 `coastCycle` 也不得短於該段。
- Chromium 與 WebKit，各跑 360／375／414／768 px 真觸控；車數、命中、溢出與控件碰撞均驗。
- mutation 必須證明任何缺訊 timeout、配不到刪車、終點不停退場或跨方向共用 ID 的改動會轉紅。

## 2026-08-14 驗收結果

- 後端生命週期：40/40 契約、16/16 mutation；保存的尖峰語料 80 輪、九線雙向，ID 無重複、
  不跨方向、退場後不復活，跨輪前進與續推週期零筆違反段秒。
- Worker 持久化：使用 roster schema／D1 key v4，隔離 v3 已產生的 BR 錯誤身分與超短續推週期；空輪保留既有 ID，
  同 revision 修訂採 fresh frame，舊 frame 不覆寫較新時間軸。
- 前端：8/8 純函式 gate、17/17 mutation（包含 `dir` 故意相反時箭頭仍服從實際移動段、
  共用箭頭不得朝車尾、低倍率固定向量，以及長畫格停頓後不倒退、不超過逐段行車秒）。
- 真實瀏覽器：Chromium／WebKit × 360／375／414／768 共 8 組全通過；每組 4/4 合成官方車可畫、
  rAF 到站準時、位置單調、零 pageerror、真觸控可命中、零水平溢出與零控件相交。

## 2026-08-15 斷線三修驗收結果（分支 fix/trtc-outage-coast，未部署）

- 語料：`fixtures/trtc-outage-20260815/`——真實斷線 33 分鐘（06:27:31～07:00）的 payload
  三份＋恢復後五輪連拍。斷線期間環狀線 Y 走新北捷上游、全程新鮮，是天然對照組。
- `verify_trtc_outage_coast.mjs` 全綠：固定段秒續推在 BR/R/G/O/BL 零超車（斷線 30 分鐘重放）；
  續推節奏＝該線段秒＋固定停站秒；同段不同車耗時一致（54 段）；Y 無段秒仍走舊路徑不消失
  （已知缺口，3 次超車如實列出）；回歸重對齊把續推過頭的 motionTo 21 拉回官方 11。
  突變：退回 per-vehicle coastCycle ⇒ 超車重現；拿掉回歸分支 ⇒ 凍在續推位置。
- `verify_trtc_outage_ui.mjs` 全綠（Chromium 390＋WebKit 375＋全台同框）：⚠ 公告鈕像素級
  可見可點、內容講明「北捷訊號中斷、位置與倒數都可能不準」且排第一則；徽章轉「官方中斷
  N 分」帶警示色；車站看板警語可見；恢復後三處全部消失；只斷環狀線時不指控臺北捷運
  （逐上游分開算）；回歸 toast 真的跳、只跳一次；桌面橫幅按掉後分鐘數變了也不再彈。
  五個突變各自打紅對應斷言（含「退回讀 roster.held」重演 08-15 原始缺陷）。
- 既有三支契約 gate 維持全綠：roster 40/40＋16/16、worker 全過、frontend 8/8＋17/17。
- 遺留：端點重複出生是 worker reducer 缺陷（上方部署順序鐵則），根因調查中；
  修好後本節要補 reducer 修正的驗收結果再部署。

若未來資料源契約真的改變，先保存新原始樣本、更新這份契約與正反測試，再動產品；不得先加
推測性的刪車或重新認車規則。
