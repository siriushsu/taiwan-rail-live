-- 只給【已經套過舊版 0003】的環境補欄位用(本機 .wrangler、開發庫)。
-- 全新的庫直接套 0003 就已經含這一欄,不需要也不應該再跑這支。
--
-- 為什麼要這一欄:cron 的「沒變就不推」判定式原本只看 last_idx 與 last_delay。斷線告知
-- (notice)的出現與消失本身也是卡片內容的變化,卻沒有任何欄位記得「上一次送出去的卡有沒有
-- 掛告知」⇒ 上游恢復的那一輪,若真觀測恰好等於表定猜的那一站、誤點又沒變(準點車全程
-- delay=0,這是常態),就零推播 ⇒ 鎖屏卡片會繼續宣稱「即時資料中斷,實際動態請查台鐵官網」,
-- 直到列車真的換站為止(自強號跨站可達 20–40 分鐘)。位置其實是對的,只有那句告知是假的。
-- 對稱的另一半也一起修好:斷線【開始】時告知不會遲到一個站間才出現。
--
-- 預設 0 很重要:高鐵與支線的列永遠不掛告知(noticeFlag 恆 0)⇒ 這一項恆等 ⇒ 判定式退化成
-- 原本的兩項,不會讓它們每分鐘重推。
--
-- SQLite 沒有 ADD COLUMN IF NOT EXISTS:重跑會回「duplicate column name: last_notice」,
-- 那是預期中的無害錯誤,代表欄位已經在了。
ALTER TABLE la_bindings ADD COLUMN last_notice INTEGER NOT NULL DEFAULT 0;
