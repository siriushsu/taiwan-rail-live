-- 🔴 與 0004／0005／0006 不同:那三支是「只給已套過舊版 0003 的環境」的補丁,全新的庫套 0003
-- 就已經含那些欄位。這一支【所有環境都要跑】——0003 已於 2026-08-08 套上正式庫,README
-- 「改結構的規矩」裡那條「建表腳本還沒上線時就地補欄位」的例外從那一刻起失效,
-- 之後任何結構改動只能新開檔案。0003 不再含本欄位,新環境＝0003 + 0007。
--
-- 為什麼要這一欄:cron 的「沒變就不推」判定式看的是 last_idx／last_delay／last_notice。
-- 「停靠中」是卡片上的第四種內容變化——車停進站、開走,站序(idx)完全沒動、誤點也常常沒動
-- (準點車 delay 恆 0)⇒ 判定式三項全等 ⇒ 零推播 ⇒ 標籤永遠不會亮,亮了也永遠不會滅。
-- 這正是 0006 那一欄要修的同一種錯,只是換成另一個欄位;沒有它,停靠中這個功能結構上
-- 不可能生效(而且前後端都零訊號,因為推播根本沒送出)。
--
-- 預設 0 很重要:高鐵與支線沒有站級觀測(useObs 恆假)⇒ stoppingFlag 恆 0 ⇒ 這一項恆等
-- ⇒ 判定式退化成原本三項,不會讓它們每分鐘重推。比照 last_notice 的設計。
--
-- SQLite 沒有 ADD COLUMN IF NOT EXISTS:重跑會回「duplicate column name: last_stopping」,
-- 那是預期中的無害錯誤,代表欄位已經在了。
ALTER TABLE la_bindings ADD COLUMN last_stopping INTEGER NOT NULL DEFAULT 0;
