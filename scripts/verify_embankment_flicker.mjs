// 小於一個像素的連續移動應平順。以每幀突變的峰值量閃動，總差異仍包含合理移動。
import {execFileSync} from 'node:child_process';
import {chromium,webkit} from 'playwright';import fs from 'node:fs';
fs.mkdirSync('output/render-fixes',{recursive:true});
const source=execFileSync('git',['show','de44dbe6:rail-3d/integration/rail-structures.js'],{encoding:'utf8'}).replaceAll("'../vendor/","'../../rail-3d/vendor/").replaceAll("'./portal-clearance.js'","'../../rail-3d/integration/portal-clearance.js'").replaceAll("'./tunnel-portals.js'","'../../rail-3d/integration/tunnel-portals.js'");
fs.writeFileSync('output/render-fixes/structures-before.js',source);
fs.writeFileSync('output/render-fixes/structure-fixture.html','<!doctype html><html><body style="margin:0"><script type="module">\nimport * as THREE from \'../../rail-3d/vendor/three.module.js\';\nimport {createRailStructures as fixed} from \'../../rail-3d/integration/rail-structures.js\';\nimport {createRailStructures as before} from \'./structures-before.js\';\nconst renderer=new THREE.WebGLRenderer({antialias:false,preserveDrawingBuffer:true});renderer.setSize(800,600);document.body.append(renderer.domElement);renderer.setClearColor(\'#fafafa\');\nconst scenes={};for(const [id,create]of Object.entries({before,fixed})){const scene=new THREE.Scene();scene.add(new THREE.AmbientLight(0xffffff,1.6));const sun=new THREE.DirectionalLight(0xffffff,2);sun.position.set(50,-100,200);scene.add(sun);\n const structures=create(scene),segments=[];for(let i=0;i<30;i++){const a=[88246.73+i*5,115726.17,4.5+i*.007],b=[a[0]+5,a[1],a[2]+.007];segments.push({a,b,groundA:0,groundB:0,scale:1,bridge:false});}structures.set(segments,[],0);scenes[id]={scene,structures};}\nwindow.run=(id)=>{const camera=new THREE.PerspectiveCamera(35,800/600,.1,2000),{scene,structures}=scenes[id],frames=[];let last=null,total=0,max=0;const gl=renderer.getContext(),buf=new Uint8Array(800*600*4);\n for(let i=0;i<80;i++){const x=88246.73+65+i*.00012;camera.position.set(x,115726.17-20,12);camera.up.set(0,0,1);camera.lookAt(x+12,115726.17,1.8);camera.updateMatrixWorld();renderer.render(scene,camera);gl.readPixels(0,0,800,600,gl.RGBA,gl.UNSIGNED_BYTE,buf);\n  if(last){let n=0;for(let j=0;j<buf.length;j+=4)if(Math.abs(last[j]-buf[j])+Math.abs(last[j+1]-buf[j+1])+Math.abs(last[j+2]-buf[j+2])>12)n++;frames.push(n);total+=n;max=Math.max(max,n);}last=new Uint8Array(buf);\n }return{total,max,frames,vertices:structures.stats.vertices};};window.ready=true;\n</script></body></html>\n');
const rows=[];for(const [engine,type]of Object.entries({chromium,webkit})){const b=await type.launch(),p=await b.newPage();p.on('pageerror',e=>console.log(e.message));await p.goto('http://127.0.0.1:53723/output/render-fixes/structure-fixture.html');await p.waitForFunction(()=>window.ready);const before=await p.evaluate(()=>run('before')),after=await p.evaluate(()=>run('fixed'));await p.screenshot({path:`output/render-fixes/${engine}-bank-fixture.png`});const pass=after.max<before.max*.35&&after.total<=before.total*1.1;rows.push({engine,pass,before,after});console.log(engine,pass,JSON.stringify({before,after}));await b.close();}fs.writeFileSync('output/render-fixes/flicker.json',JSON.stringify(rows,null,2));if(rows.some(r=>!r.pass))process.exitCode=1;
