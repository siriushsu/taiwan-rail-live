// 在出貨樹啟動獨立靜態站，驗沉浸的可逆操作；不依賴呼叫者的預覽伺服器。
import fs from 'node:fs';import path from 'node:path';import{createServer}from'node:http';import{spawn}from'node:child_process';
const root=process.cwd(),types={'.html':'text/html','.js':'text/javascript','.mjs':'text/javascript','.css':'text/css','.json':'application/json','.png':'image/png','.svg':'image/svg+xml','.webp':'image/webp','.woff2':'font/woff2'};
const server=createServer((req,res)=>{const u=new URL(req.url,'http://localhost');if(u.pathname.startsWith('/api/')){res.writeHead(503,{'content-type':'application/json'});return res.end('{}');}let file=path.resolve(root,'.'+decodeURIComponent(u.pathname));if(!file.startsWith(root+path.sep)&&file!==root){res.statusCode=403;return res.end();}if(fs.existsSync(file)&&fs.statSync(file).isDirectory())file=path.join(file,'index.html');if(!fs.existsSync(file)){res.statusCode=404;return res.end();}res.setHeader('content-type',types[path.extname(file)]||'application/octet-stream');fs.createReadStream(file).pipe(res);});await new Promise(r=>server.listen(0,'127.0.0.1',r));
try{for(const [script,engine]of [['verify_view_controls_immersive.mjs',''],['verify_ground_size.mjs','chromium'],['verify_ground_size.mjs','webkit'],['verify_view_btn_side_entry.mjs','']]){const code=await new Promise(resolve=>{const p=spawn(process.execPath,['scripts/'+script],{cwd:root,stdio:'inherit',env:{...process.env,BASE_URL:'http://127.0.0.1:'+server.address().port,ENGINE:engine,MUTATE:''}});p.on('error',()=>resolve(1));p.on('exit',resolve);});if(code!==0){process.exitCode=1;break;}}}finally{server.closeAllConnections();await new Promise(r=>server.close(r));}
