# 台鐵快車「跳站位置」推估模型（v1，2026-07-25）

> 這份是模型規格＋驗證方法論＋已知限制。要重建或修改模型，先讀完本檔。
> 相關：`研究_快車跳站校正_2026-07-24.md`（資料源盤點、保留期、扣點基準）。
> 程式：`scripts/build_pass_obs.mjs`（建模）、`index.html` 的 `buildObsProfile`（前端）、
> `verify_pass_obs.mjs`（驗收 21 項）、`eval_pass_obs.mjs`＋`diag_station_cal.mjs`（方法論實驗）。

## 1. 問題

台鐵自強、莒光等快車會**通過不停靠的小站**。官方時刻表只給停靠站的到離時刻，通過站沒有時刻。
原本前端用「梯形速度曲線」（加速→巡航→減速）在停靠站之間內插，通過站的位置與時刻都由這條
曲線算出來。問題是梯形只知道「這段有多長、要跑多久」，不知道這班車在這段路實際怎麼跑
（坡度、曲線限速、路線分歧減速、司機運轉習慣）。

目標：用實際觀測校正通過站的位置，且**不動已知為真的東西**（停靠站表定時刻、端點）。

## 2. 資料

- 來源：TDX `api/historical/v2/Historical/Rail/TRA/LiveTrainDelay`（逐站看板事件）
- 快取 60 個營運日（2026-05-27～07-25），**但只採最近一次改點之後的 25 天**（見 2.1）。
  每日全台鐵約 7.5 萬筆、13.5 MB（gzip 0.98 MB）、約 0.09 點/日
- 保留期：可回溯至 2025-01-21（550 天；是否滾動未定論，見前一份研究筆記）
- 快取：`.cache/tra_hist/YYYY-MM-DD.jsonl.gz`（不進版控，可重抓）

### 2.1 班表世代：改點前的觀測一律不採（`SHIFT_DATE`）

**2026-07-01 台鐵改點**，公告明文含「部分直達車調整停靠站」。改點會改跑段邊界與歷時，
改點前的觀測描述的是另一份班表。

**失效機制（這是重點，因為所有既有閘門都照不到它）**：車次時刻整批平移時，跑段兩端的時刻
是「表定＋觀測 `DelayTime`」而 `DelayTime` 是相對**舊**班表算的（多半≈0），於是 `tDep` 與
`dur` 都貼近表定；但通過站的 `first` 是真實時間戳 → **全部 knot 的 f 一起被平移 δ/dur**。
結果逐段速度、單調性、跑段歷時、端點誤點漂移閘門**全部照樣合格**。
實例：車次 107 南港發車 07:10→06:44（−26 分），改點前資料算出的 f 比改點後大 0.69，
山佳一站差 **1803 秒 ≒ 50 公里**，而它的跨日 MAD 只有 22s ＝ 一個「高度一致的錯值」。

量到的實際污染（改點前那版產物 vs 只採改點後）：真污染節點 179／12294（1.5%），
Δ 中位 9.7s（224 m）／p90 36.9s／max 76.5s（被 `madSec=90` 在秒量綱封頂）；
乾淨對照組（未污染的 A 錨點）本底 2.3s ＝ 淨增量約 7s。
獨立複核（第三個 agent 自建探針、先驗證能逐字重現產物）得到 n=135、中位 10.1／p90 36.9／
max 76.5 ＝ 分布逐位吻合。**MAD 閘門是這個上界的守門者**：`--mad=100000` 重跑後 max 衝到 263.2s。

**「污染是差異、還是真的傷害？」——已用留出配對檢定定案：是傷害，且劑量隨改點後資料稀疏而暴增。**
真值＝評估集（07-18～24）的第一層，兩個預測方都不含評估日，唯一差別是有沒有把改點前 35 天丟進池子：
改點後有 17 天可用時 14.8s vs 14.4s（配對中位 +1.2s）／10 天 17.6 vs 15.1（+1.7s）／
**只有 4 天時 28.1 vs 19.7（配對中位 +3.8s、平均 +11.6s；污染節點本身 29.0 vs 16.0）**。
而且混世代在 4 天情境**覆蓋率反而更低**（82.6% vs 85.1%）——舊資料撞逐日非單調（rejMono 4222 vs 41）
與最終速度清洗（943 vs 132）被丟掉。**兩邊都輸，這就是不留「天數太少就保留舊資料」退路的直接證據。**

「污染量低於噪音」那把尺（跨日 MAD 中位 16s）曾被懷疑對「中位之差」過寬鬆，實測相反：
把改點後 25 天切成互不重疊兩半各自建模、量同一節點兩邊的 f 差，地板是 13.2s（p90 44.7）；
按樣本天數配平後 n=3→19.2／n=4→15.8／n=5→14.5／n≥7→8.2s，**每一格都高於污染節點同格的 Δ**
（n=4→10.8／n=5→11.0／n≥7→9.7s）。小樣本中位本身的標準誤就 ≥ MAD，所以那把尺偏保守。

修法與代價（實跑，非估）：`SHIFT_DATE = '2026-07-01'`，`loadObs` 之後把早於它的日子整批濾掉。
第一層占比 45.3% → **44.2%**（少 139～168 個錨點，幾乎全由第二層接手）；
總覆蓋率 **88.2% → 88.2%**（節點數在兩次獨立量測裡一次 −1、一次 +18；四捨五入後不變，
方向是「持平或略好」，別宣稱「有代價」）。
選絕對日期而不是「把分層窗收成 [14,25]」：相對天數在資料窗變長後又會跨過改點日，
且下次改點要重新算；絕對日期只改一個字串。**不留「天數太少就保留舊資料」的退路**——
那等於保留上述無聲錯值；樣本不足時該節點自然過不了 `minDays` → 退梯形，那才是安全側。
⏰ **台鐵慣例 4／7／10 月＋年底改點，改點後必須更新 `SHIFT_DATE` 並重建。**

### 2.2 已知壞日，與「日健檢」的結構性盲點

`loadObs` 目前只用「>10 分鐘沒有任何資料」判斷斷抓（記進 `model.json` 的 `holes`）。
這對**全時段稀疏**型故障完全照不到：

| 日期 | 症狀 | 現行偵測 |
|---|---|---|
| 2026-07-20 | 中間斷抓 95.5 分 | ✅ 抓到（記進 holes） |
| **2026-07-11** | 只有 20,300 筆（60 天中位 71,992 的 **28%**）、311／780 車次，但 00:00–23:59 都有資料、最大空窗僅 2.2 分 | ❌ **完全照不到** |

實測代價（在時效性實驗裡）：含 07-11 的 7 天視窗第一層 5109 個，把它換成 07-10 是 5402（+5.7%），
評估誤差 17.0s→16.1s ＝ **3.5c 表中「7 天比 14 天差」的非單調有約 0.9s 是這天造成的、與時間距離無關**。
07-11 落在現行產物採用的窗內；它的樣本是「少」不是「錯」，但板面稀疏時 `first` 有偏晚的風險（未量測）。

→ **建議加一道日健檢**：當日筆數／車次數低於 60 天中位的某個比例（例如 50%）就記進 `holes`
並在統計裡標示。成本一行，且是現行偵測邏輯結構性缺的那一維。

### 資料語意（實測，不是文件寫的）

| 欄位／現象 | 實測 |
|---|---|
| 一站一車的紀錄數 | 中位 **1** 筆（通過站有 49% 只有 1 筆）→ 停站時間普遍**觀測不到** |
| `first`（板面首次出現）− 實際到站 | **−10s**，站級離散 p10 −23／p90 **−2s**，型態依賴（短停−長停）−4s |
| `last`（板面最後出現）− 實際離站 | +71s，但尾巴很肥（p90 +391s）→ **不可用作離站估計** |
| `(first+last)/2` − 表定中點 | +32s，但**隨板窗筆數劇烈漂移**：n=1 −38s／n=2 +23／n=3 +34／n=4 +112（極差 150s） |
| `DelayTime` | **整分**解析度、**整車一個值**（不是逐站獨立量測）→ 只能定位整車偏移 |
| `SrcUpdateTime` vs `UpdateTime` | Src 每日 3 萬個不同值、相鄰 gap p50 2s；Update 每日僅 1400 個值、98% 集中同一秒位、gap p50 60s → **時刻一律用 SrcUpdateTime** |
| 列 `first` 排序 | **原始檔不是時間序**（早期分析因此把潮州發車看成 18:08 而非 18:25，整輪報廢） |

## 3. 核心設計決策

### 3.1 用「跑段內時間比例 f（‰）」當模型的輸出

不存絕對時刻、不存速度，只存「這個通過站落在跑段的第幾成時間」。理由：

- 比例是**無單位量** → 天然免疫當日誤點（整段平移不影響比例）
- 免疫里程近似誤差（離線幾何 vs 官方里程差幾 %，比例不受影響）
- 前端只需一次乘法就能還原時刻，且**跑段端點的表定時刻完全不動**

### 3.2 通過站的時刻估計用 `first`，不用中點

早期版本用 `(first+last)/2 − 32s`。改用 `first − 站級校準偏移` 後，同一批資料的品質指標：

| 剔除項（品質壞才會被剔） | 中點 | first |
|---|---|---|
| 跨日離散過大 | 1259 | **9** |
| 逐日不合物理 | 1620 | **46** |
| 跑段歷時異常 | 24 | **0** |
| 逐日非單調 | 86 | 25 |

中點會漂是因為板窗（`last−first`）寬度不定：只有 1 筆時中點＝那一筆，有 4 筆時中點被拉到窗中央。
`first` 的語意穩定（板面第一次出現），偏差只有 −10s 且極差 15s。

### 3.3 真停靠站的端點用「表定 ＋ 該站實測 DelayTime」

不用時間戳。判準是**與兩個候選都無關**的物理量：算出來的節點速度有多少比例不可能
（>165 km/h 或 <8 km/h）。時間戳版 7.8%、表定＋誤點版 **3.8%**。

### 3.4 三道守門（丟點不丟整段）

- **板窗守門**：`last−first > 240s` 或 `n ≥ 5` → 不當瞬時通過點（隱藏停站、起站長掛上板）
- **端點誤點漂移**：跑段兩端誤點差 ≥2 分的日子不採（runtime 只有整車一個誤點值，
  結構上重現不了逐段誤點）
- **同號變體**：同一份班表內有多筆同車次號（加停變體，實測 434／270／281／324／4041）。
  初版整批排除 → 這 5 個長途車次**整車 0 資料**（281 一車就有 116 個通過站）。
  改法：各變體的站清單其實**完全相同**、只差 1–2 站的停靠標記 → 取第一筆處理、
  把分歧站當通過站（`first`＝到站時刻，停或不停語意相同）→ 跑段結構一致、f 可共用。
  救回 381 個槽位。站清單真的不同才整批排除。
- **站名別名**：班表帶括號註記（「新城 (太魯閣)」）而 feed 用本名（「新城」）→ 名稱 join 全滅。
  守門用**座標**不用「名稱不撞」——班表本身同時有「新城」與「新城 (太魯閣)」兩種寫法，
  用不撞名當條件會把真別名擋掉；座標同點才是「同一個站」的直接證據
  （「左營(舊城)」去括號後 feed 沒有該名，自然不會誤併）

### 3.5 兩層合成

**第一層（A，該車次自己的實測）**：取 f 的跨日中位，過閘門後採用。
`6157` 個節點／`252` 車次／占通過站槽位 **44.2%**（母體 13932）。閘門：
`minDays≥3`、跨日 MAD ≤90s、逐日與中位皆須合物理速度、逐段單調、**離群日剔除**。

**離群日剔除（Hampel，k=3）**：`|f−中位| > 3×MAD` 的那天不採（剔到不足 `minDays` 就整批不剔）。
為什麼剔「f 的離群」而不是「誤點大的日子」——f 是跑段內的**時間比例**，整車誤點 20 分但段內
正常跑時 f 完全正確，剔大誤點日會丟掉有效樣本。量測支持：被剔樣本的當日誤點 p99 19 分
vs 全體 13 分、誤點≥10 分佔比 2.3% vs 1.7%（富集 35%），但 p50／p90 完全相同＝主體是正常變異。
判準要用逐日**被剔比例**（out/in）不是絕對數——絕對數會被分層窗結構帶著走（91.8% 的節點只用
最近 14 天樣本，被剔的當然幾乎全落在那 14 天）。改用比例後每天都在 10.2%～13.0%、
標準差 1.2pp ＝ **沒有任何一天是事故日尖峰**。另外 `dlyDrift≤2 分` 本來就在擋事故：
拿掉它，進檢驗樣本 55207→67863、誤點≥10 分佔比 1.7%→2.2%、p99 13→16 分。

**分層時間窗** `[14, 30, 60, 全部]`：近期樣本夠 3 天就只用近期，不足才往前擴。
時效性優先（改點、路線工程、運轉調整都讓舊資料失真），覆蓋率兜底。實際用量與品質：

| 用到的窗 | 節點 | 天數中位 | MAD 中位 | MAD p90 | 訊號中位 |
|---|---|---|---|---|---|
| 14 天 | 5794（91.8%） | 10 | 16s | 38s | 41s |
| 30 天 | 412 | 4 | 21s | 52s | 67s |
| 60 天 | 103 | 4 | 19s | 53s | 62s |

→ 往前擴收到的節點確實較不穩，但訊號也更大（訊號/噪音仍 >2），且只佔 8.2%＝淨正面。

> MAD 閘門刻意放寬到 90s（原 45s）。稽核實測：閘門越緊，**混合後的生產誤差反而變差**
> （MAD 20→42s、45→34s、∞→33s），因為被剔掉的難點會退回最差的模型（梯形）。

**第二層（B，跨車次路段速度表）**：`A|B|邊界型|車種` → 中位速度，`1581` 鍵
（每鍵樣本數中位 74 筆、p10 21、最大 1923）。
邊界型（PP／SP／PS／SS）用修正係數換算（SP 1.269、PS 1.21、SS 1.466）。
在 A 錨點之間按段時間比例分配 → 保證單調。填 `5985` 個槽位。

段時間查不到速度表時，補法由好到壞三段式：
1. **同跑段的實測速度水準**（把查得到的段還原成等效 PP 速度後里程加權平均，再套該段的邊界型修正）
   —— 梯形的速度水準只比等速好 5 秒，而同跑段的實測水準是這條路線這個時段的真實速度
2. 整個跑段沒有任何段查得到 → 退梯形曲線的隱含時間
3. 連梯形都建不出來（不可行段）→ 純里程比例

**兩層皆無**（1638 個）不寫進產物 → 前端該段照舊用梯形（零資訊不假裝有資訊）。

### 3.5b 窗長的邊際效益（決定「要抓多久」）

| 觀測窗 | 第一層節點 | 占槽位 | 兩層皆無 | 覆蓋率 |
|---|---|---|---|---|
| 7 天 | 5294 | 38.0% | 1633 | 88.3% |
| 14 天 | 5911 | 42.4% | 1451 | 89.6% |
| 30 天 | 6343 | 45.5% | 1402 | 89.9% |
| 59 天 | 6445 | 46.3% | 1391 | 90.0% |

（本表為「清洗前」計數、且是**改點過濾之前**的實驗；現行產物只採改點後 25 天。）
**30 天之後幾乎持平**：59 天只比 30 天多 102 個節點。

### 3.5c 時效性：資料不會慢慢壞掉，只在改點那天整批失效

時間分割實驗（固定評估集＝07-18～07-24，各訓練視窗**皆 7 個連續營運日**、星期組成天然對齊）：

| 訓練視窗距離 | 制度 | 交集節點誤差中位 | p90 |
|---|---|---|---|
| 純梯形（完全不用實測） | – | 45.0s | 165.0s |
| **距離≈0 真地板**（交錯 7 天 vs 交錯 7 天） | 改點後 | **13.5s** | 44.6s |
| 7 天 | 改點後 | 17.0s | 58.7s |
| 14 天 | 改點後 | 15.8s | 57.9s |
| 18 天 | 改點後 | 16.2s | 62.1s |
| 21 天 | **跨改點** | 25.2s | 99.0s |
| 24 / 28 / 35 / 42 / 52 天 | 改點前 | 36.2 / 35.6 / 33.9 / 34.7 / 37.0s | ~125s |

（「距離 0＝自我預測」那條是 0.0s，只能當地板參考、不是公平對照，所以另設交錯 7 天的真地板。）

**這不是漸進衰減曲線，是一個階梯。** 決定性的對照：同樣 14 天距離，
同制度 **16.5s** vs 跨改點 **37.4s**（2.3 倍）；同制度從 14 天拉到 24 天只 +1.3s。
「改點沒打壞的 46 車次」子集：7 天 14.8s → 52 天 17.5s，OLS 斜率 **+0.085 秒／天**
（30 天累積 2.6s，遠小於真地板 13.5s ＝ 一個月的資料老化體感不可見）。

改點不重建的代價（實測 2026-07-01 那次）：第一層誤差中位 16s→35s、p90 58s→126s、
**20% 的車次**逐車次中位 >60s、第一層節點 −28%（覆蓋 87.7%→77.5%，約 1400 個跑段退回梯形）。
35s 已逼近純梯形 45s ＝ 模型價值大半被吃掉。
⚠️ **講「吃掉幾成」必須指明框架**：主表框架 (45.0−37.3)/(45.0−15.8) = **76% 損失**；
對照組框架（僅取兩邊都有 A 的交集節點，梯形基準 56s）= **53% 損失**。同一份文件裡兩組數字不可混用。

> ⚠️ 這個實驗的真值與預測同源（都出自 `first`），所以**只能讀成「跨視窗可重現性差異」，
> 不能宣稱絕對精度**（見第 4 節）。改點事件 n=1，「+19 秒」是這一次的量級、不可外推到每次改點；
> 改點的存在性與方向則有多路佐證（同距離異制度 2.3 倍、逐車次雙峰、覆蓋率 −28%）。
> 另：「各視窗天數嚴格相同」在**筆數**上並不成立——7 天視窗含 2026-07-11（見 2.2），
> 值約 0.9s；表中改點後三格（7／14／18 天）的排序差異在這個量級內，不要當成趨勢讀。
> 還有一個上限：改點後只有 25 天資料，所以「純改點後不衰減」這句話的實測範圍只到 **18 天**。

### 3.6 最終速度清洗

合成後對每個跑段的完整節點序列複驗速度（官方里程、上限 135 km/h），
違規時丟「刪掉後局部速度最接近該車種巡航速度」的那個中間節點，迭代到全合格。刪 `157` 點。

### 3.7 前端（`buildObsProfile`）

- **Fritsch–Carlson 單調三次內插**（PCHIP），真停靠站端點斜率 0 → 起停平滑、通過站速度連續、不過衝
- **第二道獨立防線**：節點對**軌道投影距離**複驗（build 端用的是官方里程，兩者刻意不同源）。
  起停相鄰段上限 115 km/h、中段 135 km/h，超標丟節點
- **導數尖峰夾限**：PCHIP 的 m 上界是 3×min(相鄰割線)，快慢段交界會讓瞬時速度衝到割線的
  1.4 倍以上（實測曾 228→266 km/h）。逐區間解導數二次式極大值，超標就等比縮小兩端 m
- **退回**：缺檔、無觀測、清洗後節點歸零 → 一律退回原梯形，行為與改動前逐點相同

產物：`data/tra_pass_obs.json`（164 KB，`12294` 節點／`284` 車次）。
前端實際生效 `13034` 個跑段（總 33990）。

**覆蓋率一律數最終產物**：合成當下的 `srcCount` 是清洗前的計數，之後還有 3.6 的速度清洗會刪節點。
兩者差 1.3 個百分點（90.0% → **88.2%**）。腳本現在兩個數字都印，文件與對外說法一律用清洗後的。

## 4. 驗證方法論（這節最重要）

### 4.1 兩個被推翻的判準

**(a) 留一日交叉驗證（同源）＝無效。** 早期用「訓練 6 天、預測第 7 天的實測 f」量誤差，
得到「梯形 51s → 實測 29s（改善 44%）」。**這個數字不能用**。稽核的安慰劑檢定：
對通過站觀測注入憑空的每(車次,站)固定偏差 ±120s，
**梯形基準從 52s 惡化到 78s，而實測層完全不動（33s），「改善率」反而從 36.8% 漂亮到 58.3%**。
因為真值與預測同源（都由同一批時間戳推出），這個指標只量到**跨日可重現性**，不是精度。

**(b) 遮蔽真停靠站（用官方 DelayTime 當真值）＝對通過站零資訊。**
把短停靠站（dwell≤60s）遮蔽成偽通過站，真值取「官方表定＋該站 DelayTime」。
跑完發現：**「照官方表定排點」這條基準線的誤差中位是 0s**（p75 34s、p90 60s）。
因為真值＝表定＋整車一個誤點值，跑段內的時間結構完全來自表定 → 判準其實是在問
「模型多接近表定」，而梯形本來就在逼近表定，天生占優。實測結果：
A 層 41s、梯形 37s、表定 0s（n=27132）。這條線只能當 sanity floor，不能當精度證明。

不過它的**牙是真的**（本專案管線自測，不是引用稽核）：注入 ±120s 安慰劑後，
模型誤差 44s → **95s**（A 層 41 → 110s），且物理閘門把採用節點從 16473 剔到 5496。
也就是說：模型若真的亂跑，這條線抓得到；它抓不到的是「比表定好一點還是差一點」。

**結論：這份資料裡不存在獨立於 `first` 的通過站真值**——那正是我們要推估的未知量。
承認這件事，才能誠實地說「能宣稱什麼」。

### 4.2 可誠實宣稱的：訊號對噪音比

不需要獨立真值。對每個第一層節點量兩件事：

- **噪音**＝同一(車次,站)跨日的 f 離散（MAD 換算秒）：中位 **16s**、p75 26s、p90 39s
- **訊號**＝實測中位與梯形的差（秒）：中位 **42s**、p75 76s、p90 169s
- **60.2%** 的節點，訊號超過噪音的 2 倍；訊號/噪音比中位 2.7

跨日高度一致卻與梯形系統性相差 42s → 這個差異不是隨機噪音。剩下的替代解釋只有
「`first` 有系統性量測偏差」，而那已被排除（下一節）。

> 窗從 7 天擴到 59 天後，噪音 13→16s、比值中位 3.6→2.7。**不是模型變差**：跨日離散裡有一部分
> 是真實的日間差異（週末、季節、工程），窗越長收得越多；同時多收進來的邊緣節點本來就較不穩
> （見 3.5 的分層窗表）。覆蓋率與單點穩定度在此權衡，選擇是「覆蓋優先、以 MAD 閘門守底」。

**比較基準更嚴的一版**（7 天版所測，59 天版未重測）：留一節點交叉驗證
（拿掉某個節點、用剩下的節點重建 PCHIP 曲線去預測它）誤差中位 **15.5s**，
而「梯形＋該跑段的 oracle 最佳整體平移」是 **37.3s**——連給梯形一個它現實中拿不到的
最佳偏移補償，實測曲線仍好一倍以上。這條比「訊號/噪音」更接近使用者體感的精度指標。

### 4.3 排除「訊號其實是量測偏差」

把訊號分解成「站級成分」與「車次殘差」：

| 成分 | 量 | 能不能用量測偏差解釋 |
|---|---|---|
| 站級（同一站所有車次的共同偏移） | 中位 32s、p90 79s（最大：和仁 165、竹南 130、和平 124） | **不能**。真停靠站有官方答案，逐站量 `first` 的偏差只有 p10 −23／中位 −9／**p90 −2s**，僅 5 站超過 30s。量測的站級偏差是 ±10s 級，解釋不了 32s |
| 車次殘差（扣掉站級後） | 中位 **28s**、p90 146s | **不可能**。站級量測偏差對同站所有車次一視同仁，照不到「同一站不同車次行為不同」 |

站級成分大的站有一致的地理解釋：和仁／和平／崇德在北迴線山區隧道群，竹南是山海線分歧，
花壇在縱貫線彰化前。並已對其中 31 個樣本足夠且偏差 ≥10s 的站做**逐站校準**
（校準後站級成分 34→32s，幾乎不動 → 佐證那不是量測問題）。

### 4.4 前端行為驗收（`verify_pass_obs.mjs`，21 項全過）

判準刻意取自與實作無關的來源：artifact 檔本身、HEAD 舊版行為、外部物理常數、不變式。

- 與基準（HEAD）逐點比對：11.9 萬取樣點；缺檔時**逐點完全相同**（差異 0）
- 端點不變式：80 車次的跑段起訖表定時刻異動 **0**
- 只作用於台鐵：其他系統 obs 段 0；高鐵／林鐵 4220 點與基準**完全相同**
- 物理：12.7 萬個節點對，中段最快 134.9／起停段 114.9 km/h（超標 0）；
  取樣瞬時最快 **154 km/h**（基準梯形 114，改動前曾達 266）
- 反解一致性 `profProgToTime∘profProgToTime⁻¹`：最大來回誤差 **0.0 公尺**
- 效能 2ms vs 基準 2ms（4000 次 `trainPos`）；Chromium＋WebKit＋手機 390 皆零 console 錯誤

## 5. 已知限制（都不影響安全性，但影響覆蓋率與精度上限）

1. **資料源的硬天花板：92 個站永遠沒有觀測**。班表用到 244 個站名，TDX LiveTrainDelay
   只回報其中 **152** 個。缺的 92 站（八堵、暖暖、汐科、百福、浮洲、南樹林、烏日、五權、
   嘉北、水上、竹田、崁頂、內惟、鼓山、三塊厝、新馬、牡丹…＋內灣／六家／平溪／集集全線）
   **StationID 與 feed 完全不重疊 ＝ 不是站名別名問題**（已用 StationID 逐一比對）。
   落在這些站上的通過站槽位佔 41.8%。它們靠第二層（同跑段的實測速度水準）填補；
   連跑段內任何一段都沒量到的 **1638 個槽位（11.8%）** 維持退梯形——
   那是這個資料源在物理上無法提供的資訊，寫進去只是複製梯形。
2. **相鄰路段層的直接覆蓋只有 45.2%**（205／454 個相鄰站對有實測速度）。
   與「通過站覆蓋 88.2%」不矛盾：一個跑段橫跨多個小段，只要其中幾段量到就能推出該車在
   這段路的速度水準再分配。逐路段／逐車次明細見 `研究_跳站校正覆蓋明細_2026-07-26.html`。
3. **訪次未切分**（稽核 M7，未做）：同一 (車次,站,日) 的兩次到訪會被併成一筆，
   `dlyFirst` 可能取到前一日運行的誤點。實測發生率約 0.13%，失效時多半整段被單調閘門丟掉（fail-safe）。
4. **第二層邊界修正用單一速度比**（稽核 S3）：實測 `v_PP/v_SS` 隨段長從 1.655（2–4km）
   變到 1.421（7–12km），單一係數在兩端各錯 15–25s/段。
5. **`data/tra_station_of_line.json` 鳳鳴 cumKm=54.4 可疑**（稽核 S7）：
   鶯歌–鳳鳴 5.0km／鳳鳴–桃園 3.2km 與幾何（約 2.75／5.4km）方向相反，496 個相鄰 pair 只有這 1 對異常。
   影響 `鶯歌|鳳鳴` 段速樣本高估約 80%。只有幾何證據，未對官方公告。
6. **2026-07-20 有 95.5 分鐘斷抓**（08:06–09:42），該日樣本略少。
7. 台鐵以外的系統完全不套用（高鐵無歷史逐車誤點介接；捷運另有 `metro-real-times` 管線）。

## 6. 維護

```bash
# 重抓班表＋重建校正（每週；14 天班表窗到期前必跑）
npm run fetch-schedule        # fetch → densify → build_pass_obs
npm run check-schedule

# 只重建校正（班表沒動時）
node scripts/build_pass_obs.mjs

# 驗收（會自建 HEAD worktree 當基準）
node verify_pass_obs.mjs

# 方法論實驗（要重驗第 4 節的結論時才跑）
node eval_pass_obs.mjs                 # 遮蔽判準＋表定基準線（會印出「表定中位 0s」＝判準無效的證據）
node eval_pass_obs.mjs --placebo=120   # 安慰劑：注入憑空偏差,看指標會不會被騙
node diag_station_cal.mjs              # 逐站 first 偏差、型態依賴
```

### 6.1 重建頻率（有實測依據，見 3.5c）

- **常態：跟著 `npm run fetch-schedule` 每週一次就夠。** 逼你每週跑的是
  `tra_schedule_dense.json` 的 14 天窗會過期，**不是**觀測資料老化（+0.085 秒／天）。
  漏跑的代價（無改點時）：延後 4 週 +3.2s、6 週 +4.5s，全部小於節點自身噪音 13s。
- **改點：唯一真正的期限，必須在數天內重建並更新 `SHIFT_DATE`。**
  改點後累積 3～4 個營運日（`minDays=3`）就跑一次即可回血
  （純改點後的 7 天視窗即使距離 18 天仍只 16.2s）。第 14 天以後舊制日自然退出 14 天窗。
- 觸發事件（比「幾天一次」更該盯）：台鐵定期改點 **4／7／10 月＋年底**；
  「長期路線工程、新站啟用、部分直達車調整停靠站」的公告一律視為同級觸發。
  抓資料是每日增量 0.09 點、快取永久保留 → **寧可改點後多跑一次，不必為老化定期跑。**

### 6.2 驗收自身的防呆（踩過一次）

`verify_pass_obs.mjs` 的新版目錄曾預設指向一個**釘死的 worktree**，於是連兩輪「21/21 通過」
驗的都是幾個 commit 前的舊檔案，改動一項都沒被驗到；唯一線索是 G8 的統計數字跨輪一字不差。
現在預設驗 repo 本身，並加 G0／G0b：開頭印出新版與基準的 BUILD＋關鍵檔 md5，
要求新版與 repo 逐 byte 相同、基準必須「沒有 obs 產物」（＝真的是純梯形對照）。
**跑完 verify 先看 G0 那兩行印的是什麼，再看下面的綠燈。**

### 6.3 下一步：依誤點程度分層修正（已有實測訊號，未實作）

`--dlyprobe=3` 量到（改點後 25 天、1128 個準點／誤點樣本各 ≥3 天的節點）：
**誤點日比準點日平均晚 15.0 秒通過同一站**（中位 10.7s），方向性 **t≈17.6**
（795 晚／320 早），且效應隨跑段長度成長（<10 分 8.2s、10–20 分 9.9s、≥20 分 23.1s）
＝速度略低的累積效應，不是固定的起步延遲。**誤點時車跑得更慢，不是趕車。**
分組依據是官方 `DelayTime`、被量的是 `first` 導出的 f，**兩者不同源** → 這個效應量
不受第 4 節「同源指標」的限制，可以當結論用。

三條實作路徑：(1) 全網單一修正係數（最快，但吃掉跑段長度的梯度）；
(2) **按「跑段長度 × 車種」分群估係數，套用全部節點**（樣本充足、能吃到 8s→23s 的梯度，建議）；
(3) 逐節點存兩組 f（最精準，但只有 18% 的節點樣本夠）。
前端已有即時誤點（`liveDelaySec` / `easedShift` 層）可以接。
**動手前先做「誤點分鐘 vs 效應」的迴歸，決定修正函數是線性還是會飽和。**

---

改模型常數（`GATE`／`CAL`／`SHIFT_DATE`）時，第 4 節與 3.5c 的數字全部要重量一次，並在本檔更新。

## Changelog

- 2026-07-25：v1 上線（`c961fc5`＋`3561628`）。含稽核 M1／M2／M3／M5／M6／S1 修正與逐站校準。
- 2026-07-26：v3（`394c5c9`＋後續）。🔴 發現 verify 連兩輪都在驗釘死的舊 worktree（見 6.2），
  加 G0／G0b 自檢後真正驗過＝**23/23**（數字全變：obs 段 13034→13636、車次 279→284）。
  資料只採改點後 25 天（`SHIFT_DATE`，見 2.1）＋離群日剔除（Hampel k=3）：
  第一層 6157／44.2%、覆蓋 88.2%。新增時效性實驗（3.5c：資料不會慢慢壞、只在改點失效）
  與誤點分層探測（6.3：誤點日晚 15.0s、t≈17.6、隨跑段長度成長）。
- 2026-07-26：v3.1（`832c29b` 之後）。獨立稽核（第三個 agent，自建探針先驗證能逐字重現產物）
  複核 v3 的兩份分析：核心結論全部重現，並補了兩份分析都沒做的**留出配對檢定**——
  把「改點污染是差異還是傷害」定案為**傷害**，劑量隨改點後資料稀疏暴增（見 2.1）。
  另修正三處：(a) 修法的反事實代價方向（總覆蓋是持平或略好，不是「少 1 個節點」）；
  (b) 揭露 2026-07-11 是現行斷抓偵測照不到的壞日（見 2.2）；
  (c)「跨改點吃掉幾成價值」必須註明框架（76% vs 53%）。
- 2026-07-26：v2（`1a1ad27`＋`ee335f8`）。觀測窗 7 → 59 天＋分層時間窗；同號變體改「分歧站當
  通過站」救回 5 個長途車次（281／324／434／270／4041，此前整車 0 資料）；站名別名
  「新城 (太魯閣)」→「新城」（守門用座標）；段時間查不到速度表時改用同跑段的實測速度水準；
  抓取斷線後的第一筆板面不當觀測點。第一層占比 38.0% → 45.3%，覆蓋率（清洗後）→ 88.2%。
  覆蓋率統計改數最終產物，止住 1.3 個百分點的高估。verify 21/21。
