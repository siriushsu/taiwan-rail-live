# 台鐵快車跳站校正點研究（2026-07-24）

## 問題
自強號等快車連續跳過多站時，中途位置推算誤差會隨跳站數放大。TDX 有沒有「被跳過站的通過時刻」可當校正點？

## 結論摘要

| 資料來源 | 有沒有通過時刻 | 確定度 |
|---|---|---|
| ods.railway.gov.tw「鐵路時刻表資料集」（本專案 fetch_tra_schedule.py 現用來源） | **沒有** | 高（官方 devDoc PDF 逐字確認） |
| TDX v3 `Rail/TRA/GeneralTrainTimetable`（定期時刻表） | **沒有** | 高（實際呼叫 API 拿到 live 回應驗證） |
| TDX v3 `Rail/TRA/DailyTrainTimetable`（每日時刻表） | **沒有** | 高（實際呼叫 API 拿到 live 回應驗證） |
| TDX v3 `Historical/Rail/TRA/LiveTrainDelay`（歷史誤點事件，本專案 ingest_tra_delay.py 已在用但目前只取聚合統計） | **有，但是「實測事件」不是「官方時刻表」** | 高（實際呼叫 API，逐站時間戳記已驗證涵蓋跳站段） |
| TDX v3 `Rail/TRA/StationOfLine`（含 CumulativeDistance 里程） | 不是通過時刻，是靜態里程 | 高（實際呼叫 API 驗證） |

**一句話結論**：TDX（以及本專案現用的 ods.railway.gov.tw）的「靜態時刻表」類 API 都只回停靠站，沒有通過站時刻欄位——這點兩個獨立來源（官方 PDF 開發文件逐字確認＋TDX v3 API 實測）互相印證。但 TDX 的「歷史誤點事件」資料集（Historical LiveTrainDelay）性質不同：它是逐站即時回報事件的歷史存檔，**確認會包含列車不停靠站的通過事件與時間戳記**，可以拿來當「實測校正點」（不是官方排定時刻，是歷史觀測值），且本專案已經在用同一份資料源（只是目前把逐站細節丟掉、只留每日聚合統計）。

---

## 依據與原文引用

### 1. ods.railway.gov.tw「鐵路時刻表資料集」開發說明文件（官方 PDF）

來源：`https://ods.railway.gov.tw/tra-ods-web/ods/download/devDoc/8ae4cac27f4c0348017f4dbdd21d0181`
（國營臺灣鐵路股份有限公司【鐵路時刻表資料集】開發說明文件，文件版本 V1.6，生效日期 113年01月01日）

TimeInfos（停靠資訊）的完整欄位表（JSON 與 XML 兩種格式皆同）：

> 「TimeInfos　停靠資訊　內容為JSONObject Array」
> 「Route　路線　目前空值」
> 「Station　車站代碼　請見【車站基本資料集】」
> 「Order　停靠順序」
> 「DEPTime　發車時間　HH:mm:ss，例16:22:00」
> 「ARRTime　到達時間　HH:mm:ss，例16:16:00」

逐字核對：TimeInfos 只有 Route/Station/Order/DEPTime/ARRTime 五個欄位，**沒有任何 StopStatus / 通過 / 不停靠旗標欄位**。全文件（12 頁）掃過一遍，也沒有「通過」「不停靠」字樣。這與本專案 `data/tra_schedule.json` 實際資料吻合（見下方「本專案現況」）。

本專案目前的台鐵時刻表管線（`scripts/fetch_tra_schedule.py`）用的正是這個資料集，**不是 TDX**——這點與任務描述「專案用 TDX v3 API（台鐵）」有出入，特此更正：台鐵班表現況走 ods.railway.gov.tw 直接下載，TDX 在本專案目前只用於捷運資料（`scripts/fetch_tdx.py`，`Rail/Metro/*`）與台鐵誤點歷史（`scripts/ingest_tra_delay.py`，`Historical/Rail/TRA/LiveTrainDelay`）。

### 2. TDX v3 `Rail/TRA/GeneralTrainTimetable`（定期時刻表）—— 實際呼叫驗證

用專案 `.env` 內既有的 TDX_CLIENT_ID/SECRET 取得 token，直接呼叫：
`https://tdx.transportdata.tw/api/basic/v3/Rail/TRA/GeneralTrainTimetable/TrainNo/152?$format=JSON`

回應（截取關鍵部分，2026-07 實測，EffectiveDate 2026-07-01～2026-07-31）：

```
"StopTimes": [
  {"StopSequence":1,"StationID":"5050","StationName":{"Zh_tw":"潮州"},"ArrivalTime":"18:15","DepartureTime":"18:27"},
  {"StopSequence":2,"StationID":"5000","StationName":{"Zh_tw":"屏東"},"ArrivalTime":"18:39","DepartureTime":"18:41"},
  ...
]
```

`StopTimes[0]` 的完整鍵集合（原文逐字）：`['StopSequence', 'StationID', 'StationName', 'ArrivalTime', 'DepartureTime']`——**沒有任何通過/不停靠欄位**。

152 車次（潮州→汐止，自強號 3 型）全程只有 **25 個 StopTimes**，但實際路線（潮州—臺北，縱貫線南段+山線）沿途實體車站遠超過 25 站（下方 §3 的歷史事件實測抓到同一車次同一天 67 個相異車站），確認 GeneralTrainTimetable 只回「排定停靠站」，跳過的站完全不在陣列裡（不是給 0 秒或空值，是整站不存在），與本專案現況（見下方）完全一致的資料形狀。

### 3. TDX v3 `Rail/TRA/DailyTrainTimetable`（每日時刻表）—— 實際呼叫驗證

呼叫：`https://tdx.transportdata.tw/api/basic/v3/Rail/TRA/DailyTrainTimetable/Today?$format=JSON`（928 車次）

152 車次的 `StopTimes[0]` 完整鍵集合：`['StopSequence', 'StationID', 'StationName', 'ArrivalTime', 'DepartureTime', 'SuspendedFlag']`。

比 GeneralTrainTimetable 多一個 `SuspendedFlag`（該站是否因故停靠取消，0/1），**依然沒有通過時刻或停靠與否的欄位**——`SuspendedFlag` 語意是「這個已排定的停靠站今天有沒有被取消」，不是「這站是否為通過站」。站數同樣是 25（與 GeneralTrainTimetable 一致）。

### 4. 官方 TDX 使用指引（雙鐵 API 資料使用注意事項）

來源：`https://motc-ptx.gitbook.io/tdx-zi-liao-shi-yong-kui-hua-bao-dian/data_notice/public_transportation_data/rail_data`

原文：「本平臺為利各加值單位應用，透過臺鐵提供的每日列車時刻表，轉換成每日站別時刻表」

——印證 TDX 的每日時刻表本身是從台鐵「每日列車時刻表」（即與 ods.railway.gov.tw 同一份官方來源）轉換而來，兩者是同一份底層資料的不同介面，通過時刻的欠缺是**上游台鐵營運系統本身對外公開資料的限制**，不是 TDX 或 ods 哪個平台漏做。

第三方獨立佐證：GitHub 專案 `billy1125/TRA_time_space_diagram`（讀同一份 ods.railway.gov.tw 「railway_schedule」JSON、目的是畫鐵路運行圖/time-space diagram，理論上最需要通過時刻的專案）其 README 描述做法是「列車推算通過車站時間」——用 pandas **推算**（不是讀官方欄位），且明講「公開資料不等於實際台鐵的運行計畫」。這與本專案 `densify_schedule.py` 目前的做法（Dijkstra 找路徑＋距離比例內插）是同一類方法論，非本專案獨有的權宜之計，而是這份公開資料生態圈的共同限制。

### 5. TDX v3 `Historical/Rail/TRA/LiveTrainDelay`（歷史誤點事件）—— 關鍵發現，實際呼叫驗證

呼叫（本專案 `scripts/ingest_tra_delay.py` 已在用的同一端點）：
`https://tdx.transportdata.tw/api/historical/v2/Historical/Rail/TRA/LiveTrainDelay?Dates=2026-07-23&$format=JSONL&$top=1000000`

單筆記錄的欄位（原文逐字，JSONL 每行一筆）：
```json
{"TrainNo":"1","StationID":"6250","StationName":{"Zh_tw":"吉安","En":"Ji'an"},"DelayTime":3,"SrcUpdateTime":"2026-07-23T08:35:51+00:00","UpdateTime":"2026-07-23T08:36:04+00:00"}
```

實測 152 車次（同一天、同一車次，與 §2/§3 同一班車）：當天原始記錄 166 筆，依 SrcUpdateTime 排序、合併相鄰重複 StationID 後，**得到 67 個「相異連續車站」**，時間跨度約 4 小時。逐一核對 GeneralTrainTimetable 排定的 25 個停靠站之間的「跳站段」，這 67 站完全涵蓋了被跳過的中途站，例如：

- 排定 8→9：臺南(4220)→新營(4120)（跳過 5 站）——歷史事件實際依序抓到：臺南→**永康**(11:51:32Z,delay=1)→**新市**(11:59:47Z,delay=7)→**善化**(12:07:49Z,delay=11)→**隆田**(12:15:25Z,delay=14)→**林鳳營**(12:17:55Z,delay=14)→新營(12:22:23Z,delay=14)
- 排定 10→11：嘉義(4080)→斗六(3470)（跳過 3 站）——歷史事件抓到：嘉義→**民雄**→**大林**→**斗南**→斗六
- 排定 11→12：斗六(3470)→員林(3390)（跳過 4 站）——歷史事件抓到：斗六→**林內**→**二水**→**田中**→**社頭**→員林

也就是說：**TDX 的歷史誤點事件資料集，逐站都有實測時間戳記與當下誤點分鐘數，不管該站是不是排定停靠站**。這不是官方時刻表欄位，是列車即時追蹤系統（軌道電路/車輛壓軌偵測，依 `scripts/ingest_tra_delay.py` 現有註解「事件是離站壓軌觸發」）的歷史存檔，等於一份「實測通過（或停靠）時刻」資料，可以拿來校正跳站段的中途位置。

**精度**：同一站內部有多筆記錄（例：臺南 11:42:12/11:43:32/11:44:46/11:45:28/11:46:16/11:50:47，這是排定停靠站的停留輪詢），通過站（如永康）通常只有 1–2 筆、間隔約 20–90 秒；跳站段站與站之間的時間解析度約在 1–9 分鐘量級（依站距與輪詢時機而定，不是到秒精確，但遠優於目前純距離比例內插）。

**已知限制**：
1. 這是「單日單車次」的實測樣本，本次研究只驗證 1 天 1 車次，**尚未驗證跨多天/多車次的穩定度與涵蓋率**（例如是否每天都完整涵蓋每一個中途站、是否有系統性遺漏路段）。
2. 資料是「當天實際運行」，已經含當天誤點（本次樣本 152 車在善化站已延誤 11 分），若要當「準點基準」用來做位置校正，需要多日平均或另外扣掉當日 DelayTime 做正規化，不能直接拿單日原始時刻當標準運行時分。
3. 依本專案既有記憶（`monetization-cost-decisions.md`）：TDX 歷史服務**扣點依 gzip 傳輸量計費**，本次單日全台鐵 JSONL 約 1.2MB（解壓後），拉多車次多天累積會有真實 API 用量成本，需評估。
4. 本專案現有 `scripts/ingest_tra_delay.py` 已經在每天抓這份資料，但目前的解析邏輯（`group_and_sort`/`process_day`）**把逐站細節丟掉，只保留 (service_date, train_no) 一列的聚合統計**（final_delay/max_delay/events/last_station/last_seen），沒有保留逐站時間戳記——要拿來當校正點，需要新增/修改保留邏輯，不是現成可直接讀的欄位。

### 6. TDX v3 `Rail/TRA/StationOfLine`（含 CumulativeDistance）—— 實際呼叫驗證

呼叫：`https://tdx.transportdata.tw/api/basic/v3/Rail/TRA/StationOfLine?$format=JSON`

回應範例（CZ 線，逐字）：
```json
{"LineID":"CZ","Stations":[
  {"Sequence":0,"StationID":"3350","StationName":{"Zh_tw":"成功"},"CumulativeDistance":0},
  {"Sequence":1,"StationID":"2260","StationName":{"Zh_tw":"追分"},"CumulativeDistance":2.2}
]}
```

這是**靜態里程資料，不是通過時刻**，但可以取代本專案 `scripts/densify_schedule.py` 目前用 haversine 球面距離做的比例內插權重（直線距離 vs 實際軌道里程在彎道路段會有落差），是與「通過時刻」問題正交的獨立改善點，成本低、風險低，值得順手做但不解決核心問題。

### 7. TrainType 資料集

呼叫 `Rail/TRA/TrainType` 只回代碼→中文名稱對照（如 `{"TrainTypeID":"1100","TrainTypeName":{"Zh_tw":"自強(...)"}}`），**沒有各車種的表定站間運行速度/區間運行時分資料**。本專案已經有一份手 key 的 CarClass 對照表（來自 ods 官方 PDF），TDX 這份資料集不提供額外的速度/運行時分資訊，對本問題無新增價值。

---

## 建議接入點（若要做快車路徑優化）

1. **短期、低風險**：`scripts/densify_schedule.py` 的距離比例內插改用 `StationOfLine.CumulativeDistance`（實際軌道里程）取代目前的 haversine 球面距離，改善所有跳站段的內插精度（不需要新資料源，`tra.json` 建置時順帶加這欄位即可）。

2. **中期、需要新增管線**：擴充 `scripts/ingest_tra_delay.py`（或另開一支姊妹腳本，避免動到已上線的每日增量邏輯），在既有的 Historical LiveTrainDelay 逐日 JSONL 基礎上，**額外保留跳站段的逐站首見時間戳記**（目前這些細節在 `group_and_sort`/`process_day` 就被丟棄了），針對常態運行的快車車次（自強/普悠瑪/太魯閣），累積 N 天樣本，算出「各站相對起站的中位數運行秒數」當作實測校正剖面，取代（或混合）目前 `densify_schedule.py` 的距離比例內插。此法的資料源已經在專案既有 API 用量預算內（同一份每天都在抓的資料），邊際成本只是多存欄位，不是新開一條 API 呼叫。

3. **驗證未完成之處（如果要往下做，下一步該查）**：
   - 多日多車次抽樣，確認 67/25 這個涵蓋率具代表性、不是單日巧合（例如尖峰時段輪詢是否較密、離峰是否漏站）。
   - 確認 TDX StationID 編碼與本專案既有 `data/tra_station_info.json`／`tra.json` 站碼可以直接 join（本次觀察到的代碼如 1000=臺北、0990=松山，與 ods.railway.gov.tw 的站碼数字格式一致，但沒有做逐一比對）。
   - 確認 Historical API 的實際歷史回溯窗口（能抓到多久以前的資料）與逐日扣點成本，評估累積 N 天樣本的實際費用。

---

## 2026-07-25 追加實測：回溯窗口、資料密度、扣點基準

動機：另一條線要做「往回三天依實測位置還原列車」的回放功能，需先答本檔 §7 未解項
「Historical API 的實際歷史回溯窗口與逐日扣點成本」。全部數字都是經 Worker（帶正式
token）實際呼叫量到的，非推估。臨時探測端點測完已移除。

### 回溯窗口：邊界落在 2025-01-21（距今 550 天，約 18 個月）

逐日探測（每次只問一天，避開速率限制）：

| 日期 | 結果 |
|---|---|
| 2025-01-05 / 01-15 / 01-17 / 01-19 / 01-20 | HTTP 200，**0 筆** |
| **2025-01-21** | **有資料** |
| 2025-01-22 / 01-24 / 01-25 | 有資料 |

邊界銳利（不是漸稀），且早於邊界的日期回的是 200＋空陣列，不是 404——即「查得到、
但沒存」。**未確定滾動窗或固定起始日**：兩者現階段無法區分。便宜測法＝一週後重測
2025-01-21，變空＝滾動窗（每天往前推一天），仍有＝固定起始日。對回放無實質影響
（要三天，有 550 天）。

### 資料密度（2026-07-24 全日，實測）

- 全日 **74,957 筆／789 車次／152 站**；原始 JSONL **13,549,690 bytes（13.5MB）**、
  gzip 後 **980,773 bytes（0.94MB）**，壓縮比 13.8×；Worker 端單次抓取約 1.5 秒。
  → **更正本檔 §5 已知限制 3 的「單日約 1.2MB（解壓後）」**：1.2MB 量到的是 gzip
  傳輸量（與本次 0.94MB 同一量級），解壓後是 13.5MB。扣點以 gzip 計，故原數字用在
  成本估算上是對的，只是標註反了。
- 每車次筆數分布：min 5、p25 45、**median 81**、p75 132、max 454。
- 欄位（與 §5 一致，再次確認）：`TrainNo, StationID, StationName{Zh_tw,En},
  DelayTime, SrcUpdateTime(UTC), UpdateTime`——**沒有 TrainStationStatus**，
  即資料本身不標「停靠／通過」。
- 抽樣間隔（以 1237 車次為樣本）：203 筆／25 站；同站重複筆數 median 3（max 51）；
  相鄰回報間隔 min 6s／**median 62s**／max 670s。→ 實質是「約每分鐘一次的逐站快照」。
- 與自家 D1 存檔對比（同一天）：官方 74,957 筆 vs 自家 `tra_station_events` 20,419 列
  ＝**官方密 3.7×**；單看 1237 車次 官方 203 筆 vs 自家 25 筆＝**8×**。自家存檔是
  55 秒輪詢 TrainLiveBoard 的順風車寫入，會漏掉輪詢間隙的事件；官方存檔沒這個洞。

### 停靠 vs 通過可以從密度反推（本次新發現，補 §5）

資料沒有停靠旗標，但 62 秒級抽樣讓**停等時間直接可觀測**：同一站連續多筆
（1237 有站達 51 筆）＝停靠停等，可估出到站與離站；只出現 1–2 筆＝通過。
意義：回放不需要當日表定的停靠站清單，也能判斷該站是停還是過，並得到實測停等秒數。

### 扣點基準

gzip 0.94MB／日。以既有校準（90 天回填 ≈ 8 點，使用者 2026-07-17 於 TDX 會員中心
確認）反推：**約 0.09 點／日**，一年 365 天全抓約 33 點。回放功能只抓使用者實際點到
的日期、且歷史不會變（可永久快取），成本可忽略。

### 兩個操作坑（實測）

1. **`$filter` 被靜默忽略**：`$filter=TrainNo eq '1237'` 回 200 但 0 筆——不是沒資料，
   是這個歷史端點不吃 `$filter`，只能整日抓完自己過濾（與 `ingest_tra_delay.py`
   現行做法一致）。
2. **連續快速呼叫觸發 429**：7 次連打就被限速，容易誤讀成「那天沒資料」。逐日一次＋
   `--retry` 才可信；本次回溯窗口的空日結論都是在單發＋重試下複測過的。

### 高鐵：查無歷史逐站觀測，且上游結構上不可能有

- 端點實測（帶 token）：`Historical/Rail/THSR/LiveTrainDelay`、
  `Historical/Rail/TRA/TrainLiveBoard`、`Historical/Rail/TRA/DailyTrainTimeTable`、
  `Historical/Rail/THSR/DailyTimetable`、`Historical/Rail/TRA/StationLiveBoard`
  **全數 404 Resouce Not Found**。
- 官方端點目錄**未讀到**：`api/historical/swagger/v2/swagger.json` 無 token 回 401、
  帶 token 回 404（閘門層先擋，路徑實際不存在）；`api-service/...` 那組回的是 SPA
  的 index.html 不是 spec。故「除 LiveTrainDelay 外還有哪些歷史資料集」仍屬未知。
- 結構性證據（確定度高）：本專案抓高鐵只有 `Station/StationOfLine/Shape/
  DailyTimetable`（`scripts/fetch_thsr.py:48-51`），即時面只有 v2 `AlertInfo` 的
  全線營運狀態（`worker.js:11-12`），高鐵在 app 內本來就是 `sched` 模式
  （`thsr_sched`）＝**上游沒有逐車即時誤點**，沒有即時資料就不會有它的歷史存檔。
- 結論：回放的實測還原**只有台鐵**；高鐵維持表定重播（與今天行為相同）。

### 對回放功能的結論

1. 資料源不必依賴自家 D1——官方歷史同粒度、更密、且回溯 550 天，自家存檔實質是快取。
   （先前「不延長保留就永久失去過去資料」的判斷是錯的，已更正；365 天保留仍值得留，
   因為讀自家 D1 比逐日抓 13.5MB 快得多。）
2. 精度上限：約 62 秒一筆的逐站快照＋可觀測停等，優於自家存檔，也優於純距離內插。
3. 每個日期只需抓一次、可永久快取；成本 ≈0.09 點／日。
